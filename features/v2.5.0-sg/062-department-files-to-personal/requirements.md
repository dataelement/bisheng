# F062 需求

Status: `confirmed`

以已确认的 [spec.md](./spec.md) 第 1、2 节为完整需求和验收事实源。REQ-001 至 REQ-010 分别对应 AC-01 至 AC-10，包含扫描、用户路由、个人库创建、目录、冲突/版本、预览、迁移完整性、失败记录、租户隔离和共享关系保护。

用户确认：按 `user_id`；递归保留指定根目录及下级层级；只扫描部门知识库；同名文件逐个迁移、后覆盖前；本次仅交付脚本与验证，不执行真实迁移。

验收方法沿用规格第 2 节，每个需求对应一条可验证 AC；实际证据统一记录在 verification.md。脚本仅在显式 apply 下写入，不迁移外部逻辑引用文档、不跨租户。

2026-09-09 追加确认（替代原冲突跳过约定）：REQ-003 匹配到多个同名默认个人库时按库 ID 升序取最小 ID，预览显示全部候选 ID 和选中 ID。REQ-005 按来源库 ID、文件 ID 顺序逐个迁移，后迁入文件覆盖本批次先迁入的同名文件及目标原有同名文件，删除被覆盖逻辑文档及完整版本链。覆盖仅匹配同用户、同目录、同文件名，不按 MD5 覆盖其他路径内容。预览零写入；真实运行仍需 --apply。

AC-05 追加：报告必须列出目标原有覆盖文档 ID/版本 ID，以及批次中被后续覆盖的单元 ID。复制并验证新文件后重校验旧目标，持久记录后才允许永久删除旧目标；删除失败立即停止并保留当前来源和新副本，报告旧目标可能部分删除。受发布/分享引用、审批锁、非成功状态或损坏版本图保护的旧目标仍不得覆盖。


## 2026-09-10 原记录迁移修复

用户要求修复审查发现的覆盖部分删除、索引异常中断、全租户反复加载和跨脚本依赖问题。本次已获授权的修复取代旧复制补偿设计：单脚本内置必要逻辑，保留文件/文档/版本 ID 和原对象地址；同名旧链删除与来源归属变化在同一事务。索引异常、失败/超时状态和模型不同可迁入后标记解析失败。部门转个人权限切换必须成功，权限/数据库/审计失败仍停止；提交异常须查询实际归属后才决定恢复旧权限。分部门加载命中子树、执行定向复查，保留外部引用与审批保护。新建目录及旧目标对象保留审计，不清理部门库。仅本地修复与验证，不包含真实 apply 或容器更新。
