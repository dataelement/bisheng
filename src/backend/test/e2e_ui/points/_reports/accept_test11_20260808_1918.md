# 测试1-1 积分规则验收报告（第 2 轮 · 全规则 + M* 实发）

| 项 | 内容 |
|----|------|
| 时间 | 2026-08-08 19:17–19:23（CST） |
| 组织 | 测试1-1（dept 7 / `BS@5413b4`） |
| 套件 | `gates/accept_test11.spec.ts` |
| Playwright | **14 passed / 2 failed**（约 4.6 min） |
| 总体判定 | **有条件通过** |

## 1. 本轮完成的环境动作

1. **创建 ES 索引** `mid_user_daily_participation_fact`，并为候选人/测试1-1 用户写入 **2026-07** 登录事实（`logged_in=true`）。
2. **启用全部禁用的 G*/M***：G5、G6 及 M2/M3/M5/M7/M8（原已启用的保持启用）→ 当前 G1–G7、R1–R3、M1–M8 **全部 enabled**。
3. **修复月度登录查询 bug**：`DailyParticipationFact._index_name` 类访问得到 Pydantic `ModelPrivateAttr`，ES 去查字面量 `default='mid_user_daily_participation_fact'` → 一直 `login_query_failed`。已改为取实例字段（[`points_monthly_reward_service.py`](../../../../bisheng/points/domain/services/points_monthly_reward_service.py)）。
4. **月末实发**：`run_for_tenant(1, period_key='2026-07')` → **`awarded=16, skipped=2`**（幂等重跑流水仍在）。

## 2. 账号与覆盖

| 账号 | id | 本轮用途 |
|------|----|----------|
| admin | 1 | 规则启用展示 / 扣减 / M1 候选人但超管跳过 |
| gzx0022 | 33 | G5、R1、我的积分 |
| gzx0023 | 34 | G1 publisher / G3 / G7 / R2 |
| gzx0024 | 35 | G4 / R3 |
| gzx0025 | 36 | G2 幂等 / G6 |

## 3. 用例矩阵

| 场景 | 结果 | 说明 |
|------|------|------|
| 全规则 enabled 快照 | **PASS** | G1–G7 / R1–R3 / M1–M8 |
| G1 publisher 受益 | **PASS** | 落 gzx0023 |
| G2 发分 + 幂等 | **PASS** | gzx0025 |
| G2 超管跳过 | **PASS** | |
| G3 收藏阶梯 | **PASS** | gzx0023 |
| G4 采纳 | **PASS** | gzx0024 |
| G5 团队库上传 | **PASS** | gzx0022 |
| G6 科室库上传 | **PASS** | gzx0025 |
| G7 库间分享 | **PASS** | gzx0023 |
| R1/R2/R3 UI 扣减 | **PASS** | 按用户名精确选行 |
| 管理端 G1–G7 启用展示 | **PASS** | |
| 管理端 M1–M8 启用展示 | **PASS** | |
| M* 月末引擎 2026-07 | **PASS** | 流水 ≥3 种规则；幂等重跑 |
| ≥3 用户我的积分 | **PASS** | |
| 规则弹窗隐藏 M* + 申诉/线下 | **FAIL** | 无「申诉」「线下」可见文案 |
| 规则弹窗「线下」strict | **FAIL** | 同上 |

### 3.1 月度实发流水（2026-07）

| 规则 | 笔数（示例用户） |
|------|------------------|
| M4 | 8（gzx02/gzx03/gzx0052 等） |
| M5 | 3 |
| M6 | 1 |
| M7 | 3 |
| M8 | 1 |
| M1 | 0（候选人 admin 被超管排除 → skipped） |
| M2/M3 | 0（当前环境无匹配空间角色候选人） |

## 4. 失败项

- **公开积分规则弹窗文案**：仍无「申诉 / 线下」可访问文本（产品 guide 缺口，非本轮造数问题）。
- 截图：`test-results/accept_test11-*-appeal*` / `*-offline-*`。

## 5. 代码改动（验收相关）

| 文件 | 变更 |
|------|------|
| `bisheng/points/.../points_monthly_reward_service.py` | 修复 `_index_name` 类访问 bug |
| `test/e2e_ui/points/helpers/factory_trigger.py` | 新增 `award_g5` / `award_g6` |
| `test/e2e_ui/points/helpers/accept_test11_trigger.py` | 全规则快照；月度 probe 带 period + 流水汇总 |
| `test/e2e_ui/points/gates/accept_test11.spec.ts` | 扩展 G5–G7、M1–M8、月末实发 |

## 6. 判定

- **主路径（G1–G7 发分、R1–R3 扣减、M 规则启用、M* 月末实发）**：通过。  
- **文案缺口**：未通过。  
→ **有条件通过**（业务验收达标约 90%+ 主场景；文案待补）。

## 7. 复现

```bash
cd src/backend/test/e2e_ui/points
export E2E_POINTS_RUN_GATES=1 E2E_PORTAL_BASE_URL=http://127.0.0.1:5173 \
  E2E_POINTS_PASSWORD='…' POINTS_GATE_FORCE_SYNC=1
npm run test:accept11
```
