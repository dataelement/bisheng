# Filelib 文件同步接口迁移提示

原分拆接口契约已经停用，不再作为可调用接口文档维护。

请使用现行的 [Filelib 统一文件同步接口](./filelib-file-sync.md)。分类、业务域和目标知识空间由开发者 Token 的 `file_sync_rule` 统一配置；旧调用方、Token 路由白名单和业务配置均需按新文档人工切换。

本次迁移不提供兼容路由、重定向或自动配置推导。服务端升级后，继续调用已移除的分拆接口会返回 404。发布与回退步骤以统一接口文档中的“发布切换清单”为准。
