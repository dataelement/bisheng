import { HubModuleNavTabs } from '~/components/Nav/HubModuleNavTabs';

/** App chat sidebar: four hub entries with equal-width cells. */
export function SideNavModuleTabs() {
  return <HubModuleNavTabs equalWidth squareItems />;
}
