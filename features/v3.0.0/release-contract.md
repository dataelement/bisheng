# Release Contract — v3.0.0

> 本文件是 v3.0.0（应用工场）版本级领域归属与全局约束的权威来源。
> **所有 spec.md 在动笔前必须先阅读本文件。**
> 每次 spec 评审时，必须对照本文件检查一致性。
>
> 上游 PRD：《3.0 应用工场 PRD-1 专业开发者通道与应用运行时》**v2.0**（2026-08-17，21 项功能：DEV×6 / RT×7 / GOV×8）+
> 伴生《3.0 开放 API 鉴权与身份传递 PRD》**v2.3 草案**（2026-08-18，R1–R9）；技术方案见《3.0 应用工场 产品方案》§4–§5（F100–F113）。
> Spec Discovery 与 11 维代码调研锚点：[000-prd1-discovery/](./000-prd1-discovery/)（**discovery.md 已于 2026-08-17 按 PRD-1 v2.0 重排为拆分 v2**）。
>
> **编号约定**：
> - Feature 编号从 **F049** 起——F043–F048 已被 `features/v3.0.0-beta1/` 占用（本分支基于 `feat/3.0.0-beta1`，该目录与其 release-contract 在本分支内可直接查阅；⚠️它尚未合入 main）。拆分 v2 新增 **F058 / F059**。
> - 跨 Feature 不变量从 **INV-27** 起。INV-1~7 = v2.6.0 存量（继续有效）；INV-8~26 = v3.0.0-beta1（全部出自 F048 权限重写）——开发基线定为 `3.0-vibe`（含 F048），故**无条件生效**。
> - 历史编号空间：v2.5.0 INV-1~9 与 v2.5.1 INV-T1~T19 是独立编号空间且部分仍有效（部分已被 beta1 INV-24/25 替代），引用时**必须带版本前缀**；本版本裸编号 INV-N 专指 v2.6.0 → v3.0.0-beta1 → v3.0.0 连续链。

---

## 表 1：领域对象归属

每个领域对象只能有一个 Owner Feature，负责定义该对象的写入行为
（创建、更新、删除）。其他 Feature 只能"读取"或"引用"该对象。

| 领域对象 | Owner Feature | 说明 |
|---------|--------------|------|
| **ApiCredential**（平台自签发凭据底座：服务账号密钥 `bs-sak-`；**全平台唯一主体轨**，`bs-pat-` 个人主体轨已废除，F049 spec §4 决议-4） | **F049-openapi-auth-baseline** | 拥有凭据的生成、哈希存储、校验、权限位（含三扩展位 `model:invoke` / `identity:read` / `app:manage` 的清单登记）、过期、软撤销与批量撤销、最后使用时间写入。三种用途共用本底座、不分凭据类型：开放 API 集成密钥与 PRD-1 开发者 key（同一张清单、勾哪些位的差别）由管理员签发；**应用运行期凭据**由 F055 随发布自动签发 / 回收（不作产品概念、无管理入口，PRD-1 GOV-08）。F050 追加 `delegate` 位与委托范围属性，不改底座语义。会话派生凭据已随「会话 key」移出 PRD-1、归 PRD-2 |
| **ServiceAccount**（不可登录的服务账号主体：`User` 上的主体类型标识 + 租户归属 + **资源归属人**（必填自然人，伴生 PRD §4.5 定义 6）+ 登录守卫 + 选人场景排除 + 主体侧授权页） | **F049-openapi-auth-baseline** | 伴生 PRD R4 / R6。拥有服务账号的创建 / 启停 / 删除、租户关联行的声明式写入、对账豁免、选人场景默认排除、资源归属人的写入与「创建关系落归属人 + 回授可编辑」语义、主体侧资源授权页；不拥有 `User` 表本身（自然人侧行为不变）。**服务账号不得被授予管理员身份、不得被授予任何角色、不得被主动加入用户组 / 部门**（F049 AC-22） |
| **App / AppVersion / AppInstance**（托管应用本体、只增不改的版本记录 + 快照〔代码 · 能力声明 · 注入配置 · 资源档位同一快照〕、运行实例；应用态 / 审批单态状态机 PRD-1 §3.0.2） | F054-app-domain-runtime | RT-05 语义与既有 flow_version 可变指针语义相反，独立建模；能力声明（RT-04）是 AppVersion 快照的组成部分，F055 拥有其在发布管线中的冻结 / 审读 / 注入，不另立对象 |
| **ResourceTier**（资源档位，三档预置、平台超管可调规格与启用状态） | F055-app-publish-pipeline | GOV-03 档位实体 + 系统管理「资源档位」tab + 发布选档 / 入快照 / 上线终检；F054 只读（把档位映射为容器 / Pod 资源上限）；F056 只读（列表展示） |
| **「应用发布」审批场景预置**（审批中心既有 ApprovalScenario 对象上的一条平台预置流程 + 新建租户同步落库钩子） | F055-app-publish-pipeline | GOV-02。审批场景 / 分支 / 节点 / 或签对象归审批模块（F025），F055 只拥有该场景的预置写入、`tenant_admin` 审批人来源修正与 Root 租户回退超管、新建租户 seed 钩子 |
| **AppManifest**（应用包声明文件 `bisheng-app.yaml` 的对外形态：名称 / 描述 / 图标 / `runtime` / 端口 / 档位 / 能力声明 / 应用数据表结构 / 出站域名白名单） | F055-app-publish-pipeline | 发布输入的权威 = 管线校验方（F055 spec 范围边界 RT-04 段）；F053 CLI（打包 / `dev` 启动依据）与 F054 运行时（`runtime` / 端口 / 域名白名单）按同一形态**消费**、不各自定义；能力声明是其子集（RT-04） |
| **ModelCallRecord**（逐条模型调用记录：密钥 / 服务账号 / 资源归属人 / 应用 / subject / 模型 / 时间 / token 数；本版不做聚合账单） | F051-model-protocol-gateway | GOV-04 事件清单「模型调用记录」；F051 写入（含被拒调用的审计事件），F056 查询面只读；托管应用运行期经 F051 调用时应用与 subject 维度由 F055 注入契约带出；用量同时计入平台既有 token 用量口径（同一账本） |
| **ShareLink**（既有分享链接对象；本版增量 = 撤销写入 + 有效期强制生效 + share-token 会话执行主体） | **F049-openapi-auth-baseline** | F049 spec §4 决议-1 / 决议-6 i：两个免登录分享页改走 share_link 通道，两个 WS 端点接受 share-token；本 Feature 拥有本版对该对象的写行为增量，不拥有其既有创建 / 读取行为 |
| ~~（候选）AppServiceAccountToken~~ | — | **已撤销登记（2026-08-17）**：PRD-1 GOV-08 v1.8 起「应用运行期凭据不作产品概念」，它就是 ApiCredential 的一种主体绑定，由 F055 编排、F049 底座签发，不是独立领域对象 |

**规则**：
- 非 Owner Feature 的 AC 中不得出现其他对象的"创建/修改/删除"行为，只能"读取"或"调用" Owner 的 Service
- 新增领域对象时必须先更新本表

---

## 表 2：跨 Feature 不变量（INV-N）

全局业务约束，任何 spec 的 AC **不得与之矛盾**。
（INV-1~7 见 v2.6.0 release-contract；INV-8~26 见 v3.0.0-beta1 release-contract，无条件生效。）

| ID | 不变量描述 | 涉及领域对象 | 来源 spec |
|----|-----------|------------|---------|
| INV-27 | **开放 API 默认拒绝**：`/api/v2/**` 的任何调用必须先由平台签发的凭据确立主体，不得回落到任何固定/配置身份；**不存在兼容窗口、鉴权开关或迁移期放行**（伴生 PRD D1）；本次不暴露的 6 个 `/chat/*` 端点靠关闭而非加鉴权解决 | ApiCredential | F049 |
| INV-28 | **平台自签发凭据统一底座**：一切平台自签发凭据（服务账号密钥——含开放 API 集成、PRD-1 开发者 key 两种用途、以及 F055 自动签发的应用运行期凭据；平台不存在自然人个人凭据，决议-4）必须密码学随机生成、服务端仅存单向哈希（不用可逆加密）、明文仅签发时一次性展示、此后一切界面只见掩码（前缀 + 末四位）、撤销为软撤销且保留审计记录、撤销生效上界 5 秒（主动清缓存，不依赖 TTL 自然过期）。有效性判据唯一 = 未撤销且未过期，不得引入可独立漂移的状态枚举列 | ApiCredential | F049 |
| INV-29 | **服务账号主体不可登录、不进任何选人场景、授权只走主体侧**：服务账号不得通过任何登录入口获得会话；其租户归属为声明式写入、不由部门树派生，任何以部门树为真相的对账都必须结构性跳过它；不计入租户用户数配额；**不出现在任何面向人的选人场景**（含资源侧授权选人框；资源侧「已授权对象列表」仅可显示、不可选择），排除做在数据访问层、失败方向必须是"看不到"而非"泄漏"；对它的资源授权**唯一入口在其详情页主体侧**；不得被授予管理员身份、不得被主动加入用户组 / 部门 / 角色。**（2026-08-17 修订：原「仅在资源授权选人处显式 opt-in 可见」口径已被伴生 PRD v2.0 §4.7.4 推翻）** | ServiceAccount | F049 |
| INV-30 | **开放面权限评估 fail-closed（INV-19 在开放 API 上的加强）**：权限引擎不可用、评估失败或结果不可判定时，开放 API 必须返回错误，**且绝不返回未过滤或部分过滤的结果集**；不存在任何"缺身份即降级返回某个子集"的路径。本条不放宽 INV-19，只补充「部分过滤亦属失败」这一判据 | ApiCredential, PermissionGrant | F049 |
| INV-31 | **三扩展位仅模式 S 且与 `delegate` 互斥**：`model:invoke` / `identity:read` / `app:manage` 服务的本地开发三面（MCP 工具面 / 模型协议面 / CLI）一律以服务账号自身身份执行、不承载委托；签发与编辑表单中 `delegate` 与三位**硬阻断、无法同时保存**（随 F050 委托配置区交付；F049 期表单不提供 `delegate` 位、互斥结构性成立）；三面在通道入口**按权限位直接拒绝**持 `delegate` 的密钥（CLI 在 `login` 即拒），错误指向「委托专用、本地开发另签一把」，**不得实现为漏传委托头就静默落回模式 S**、也不得只回参数错误码。应用工场全册无模式 D 场景（PRD-1 DEV-02 ④ / 伴生 §4.2.4） | ApiCredential | F049（登记）；F050（互斥校验）；F051 / F052 / F053（入口拒绝） |
| INV-32 | **平台注入身份是应用取得访问者身份的唯一可信来源**：托管运行期由 app-proxy 验平台会话后注入当前访问用户，本地 `bisheng dev` 期由 CLI 迷你代理注入开发者服务账号；客户端传入或伪造的身份一律无效；应用侧不得自建登录页或自行解析凭据；不提供任何身份模拟开关（PRD-1 NFR-1.3 / RT-01 / DEV-05） | App, AppInstance | F054（app-proxy，AC-31/32/34）/ F053（dev）/ F057（auth）——2026-08-17 随 F054 spec 定稿转正 |
| INV-33 | **运行时双形态产品面一致**：compose 单机与 k8s 集群两种执行后端下，发布管线、资源档位、审批、权限、日志、崩溃自愈、待上线态成因等一切产品行为与验收**不得有任何差异**，差异一律压在编排器内部；形态在部署期二选一、不做形态间迁移与同环境并存（PRD-1 前提 4 / GOV-10） | AppInstance, ResourceTier | F054（AC-49）/ F059 / F055——2026-08-17 随 F054 spec 定稿转正 |
| INV-34 | **发布必审、无免审路径**：托管应用的首发与迭代每次发布均生成审批单、经平台预置流程审批通过后方可上线；不存在免审配置项；可见范围变更、元信息修改不属发布、不经审批、计审计（PRD-1 RT-03 / RT-04 / GOV-01 / RT-02） | App, AppVersion | F055（AC-23）——2026-08-17 随 F055 spec 定稿转正 |
| INV-35 | **资产真身恒在平台存储、删除唯经显式删除**：运行实例是可随时回收的执行体、自身不保存任何唯一数据；代码 / 应用数据的真身在平台存储；除 owner 显式删除流程外不存在任何导致应用资产被删的操作路径；已上线态不可直接删除、须先下线（PRD-1 RT-07 / NFR-3） | App, AppVersion, AppInstance | F054（AC-38/40/42/43）——2026-08-17 随 F054 spec 定稿转正 |
| INV-36 | **托管运行期检索 = 能力声明白名单 ∩ 当前访问用户可见范围**（文件级、双层过滤、fail-closed）：声明层是显式白名单收窄，用户层是按当前访问用户的逐资源鉴权（纯替换，与其在平台内自行检索集合相等）；无法取得访问用户身份 → 拒绝、绝不回退为按声明全量放行；本地 dev 与 MCP 面锚定开发者服务账号被显式授予范围（PRD-1 GOV-05 / DEV-02 / NFR-1；伴生 §4.4.6）。**唯一例外**：审批期临时预览实例注入审批人身份、平台能力按 owner 权限放行且计审计（PRD-1 NFR-1.2 审批例外条款，F055 AC-27） | KnowledgeBase, App | F052（门面，AC-19~27/46）/ F055（注入 AC-50 + 审批例外 AC-27）/ F057（retrieve）——2026-08-17 随 F052 spec 定稿转正 |

**规则**：
- 新增不变量：先在此表追加，再写 AC；候选项随对应 spec 定稿转正（INV-32~36 已于 2026-08-17 转正）
- 修改不变量：必须列出 Impacted Specs 清单，逐一回写并重新评审
- 冲突检测：若 AC 与不变量矛盾，spec 评审不通过

---

## 表 3：Feature 依赖图（拆分 v2，2026-08-17；随 spec 定稿逐行确认）

批次 A = 开放能力层（GOV-10：可独立于工场运行时层部署，且是批次 B 的鉴权 / 凭据地基）；批次 B = 工场运行时层。方案列为《3.0 应用工场 产品方案》§5.1 的 F1xx 技术特性映射。

| Feature | 批次 | 依赖（必须先完成） | 说明 | 方案 F1xx |
|---------|------|-----------------|------|-----------|
| F049-openapi-auth-baseline | A | — | **伴生 PRD 的 P0 阶段**（spec 已按 v2.1 口径定稿、★ 已过，见 `049-openapi-auth-baseline/spec.md`；design / tasks 编写中）：R1 凭据底座 + R4 服务账号主体（含**资源归属人**）+ R5 全端点接入（36 HTTP + 2 WS，6 个 `/chat/*` 不暴露）+ R6 管理界面（含主体侧授权页 v2.1 形态）+ R9 升级须知（**平台侧零迁移**）+ 三扩展位登记入权限位清单 + **开放能力层部署开关**（GOV-10；F051–F053 复用）+ 分享链接通道（含 share-token 会话执行主体）+ 四处既有缺陷修复。PRD-1 增量仅签发表单层（「本地开发工具包」分组、`identity:read` 风险提示、归属人选错风险提示）+ GOV-08 四条承诺。**不得脱离 F050 单独对外发版**（裸 `user_id` 须随 v3.0.0 移除） | F101 |
| F050-identity-modes | A | F049；伴生 AC-12「模式 D 检索集合相等」依赖 F052 检索门面 | **伴生 PRD 的 P1 身份传递**（估算 12–14 人天）：R2 两种身份模式 + 外部用户标识头 + R3 受限委托五道准入 + R7 审计双归属 + 裸 `user_id` 参数收口（含 `add_relative_qa` 死参数）+ **`delegate` 位与委托配置区**（含伴生 AC-48 互斥硬阻断、AC-46 模式 D 归属优先级、授权页「名下存在 delegate 密钥」提示、列表「委托」列）。开放能力层部署开关由 F049 拥有（F049 AC-49），F051–F053 复用，本 Feature 不承接 | F101 |
| F051-model-protocol-gateway | A | F049 | DEV-02 模型协议面：**仅 OpenAI 兼容**（Anthropic 面本版不做，方案 §3.4）、`model:invoke` 位、按名解析模型（跨服务商同名歧义规则须定）、流式、可用范围 = 租户模型管理已启用模型（GOV-05 模型项）；GOV-04 模型调用**逐条审计**带 key / 应用维度（用量表补 `api_key_id` / `app_id`；**聚合账单顺延 v3.1**）；入口拒绝持 `delegate` 密钥（伴生 AC-49 一面，INV-31） | F102 |
| F052-mcp-server-face | A | F049 | DEV-02 MCP 工具面六类工具（知识库检索 / 知识库清单 / 模型清单 / 身份组织查询 `identity:read` / 应用数据表结构与数据 / 应用状态与日志——后两类随 F054 / F055 接线）+ **统一检索门面**（文件级双层过滤 + fail-closed；伴生 §4.4.6 交付前提，v2 `POST /filelib/retrieve` 收敛到同一路径；GOV-05 运行时 / F057 retrieve 共用）+ 入口拒绝持 `delegate` 密钥（INV-31） | F105 |
| F053-dev-cli-skills | A 尾 / B | F049, F051, F052 | DEV-03 两包技能包（部署纳管 / 平台能力接线，内外同源、触发不绑产品名）+ DEV-04 CLI `login`（成功后自动 `skills sync`）/ `skills sync` / `dev` / `logs`（`deploy` 命令随 F055 启用）+ DEV-05 `bisheng dev` 本地身份注入（迷你代理注入开发者服务账号 + 本地 sqlite 同名连接注入，无 `--as`）+ CLI 安装件平台自身分发 + **DEV-01 接入信息区与一键复制**（不含 key 值）+ `login` 拒 `delegate`（INV-31） | F106（部分） |
| F057-bisheng-sdk | A 尾 / B | F052, F053；storage 依赖 F054 | DEV-07 三件套（auth / retrieve / storage）+ 开发者指南；chat / appdb 刻意不进 SDK（行业标准库 + 平台注入环境变量）。验收 3「与平台内模板同范式」依赖 PRD-2 WB-01，随 PRD-2 验收 | F106（部分） |
| F058-openapi-responses | A | F050 | **登记项。** 伴生 PRD P1「日常模式会话开放」：以**业务语义**开放工作台日常模式对话（选模型 + 可选挂知识库 / 开平台工具 / 传附件 + 服务端多轮 + 流式与非流式）+ 入参清洗 + 事件流转译 + 会话附件上传；本版仅**日常模式 × 同步**，运行模式与传输方式是两个独立维度（伴生 D11）。**对外契约形态归 design**（伴生 v2.3 **D9 改判**：PRD / spec 只写业务需求，字段名 / 事件名 / 端点路径 / 是否对标第三方契约由研发定；连带取消「官方 OpenAI SDK 可直连」这一需求，该诉求由 F051 承接）。会话列表 / 历史查询、面向同一能力的第二套并行契约均不做（D12）；伴生 PRD 要求与身份传递同期交付。**不在 PRD-1 范围**，此处补齐版本层归属（F049 spec 决议-5 ⑤ 遗留）；是否进 v3.0.0 由产品定 | F101 |
| F054-app-domain-runtime | B | F049 | 托管应用领域模型 / 应用态与审批单态状态机（PRD-1 §3.0.2）+ GOV-01 `app` 资源类型注册（后端 12 处 + 前端 3 处联合类型 + Catalog 范围表存量生效手段）+ compose 执行后端（runtime-manager，唯一持编排后端访问、backend 零 docker 依赖）+ app-proxy（版本无关鉴权代理、注入访问者身份）+ RT-01 统一入口 `/apps/{slug}` 与四类兜底页 + RT-07 资产持久 / 下线 / 重新上线 / 显式删除（前置状态闸）+ RT-08 稳定性（崩溃隔离、5 分钟自愈、发布中 / 恢复中过渡态）+ GOV-03 档位→容器资源限额落地 + per-app 数据库（SQLite / Postgres，与平台 MySQL / DM8 解耦）与附件存储 + platform 构建页「托管应用」类型 + 应用详情页壳（**WB-13 运行日志 / WB-06 数据面**）+ GOV-10 工场运行时层部署开关与未部署引导页。**体量大，spec 阶段可再拆**（编排后端 vs 领域模型 / 界面壳） | F103, F107, F104（FGA 类型） |
| F055-app-publish-pipeline | B | F054, F049, F051, F052 | RT-03 发布审批流（提交 → 冻结快照 → 密钥扫描 → 审批 → 上线终检；审批人审读视图 + 审批期临时预览实例）+ `deploy` / `logs` 服务端权限判定（`app:manage` / 归属人 owner-only / 拒 `delegate`）+ RT-04 能力声明（含 `bisheng-app.yaml` 对外形态，表 1 AppManifest）+ RT-05 版本记录与结构演进（加列自动 / 改删列显式确认 + 迁移前快照；**平台侧回滚本版不做**）+ DEV-04 deploy 管线（托管预检 / 安全扫描 / 审批 / 上线）+ **GOV-02 平台预置审批流**（部门管理员 ∪ 租户管理员或签；三项阻塞前置：`tenant_admin` 来源改用真租户管理员、Root 租户回退平台超管、新建租户同步 seed；`withdraw` 终态守卫）+ GOV-03 ResourceTier 实体 / 系统管理 tab / 发布选档 / 上线终检 / 待上线（资源不足）态 / 手动上线 + **GOV-05 能力总线**（能力声明 → 运行时注入通道；能力收回 → 明确错误态与 owner 失效提示；密钥引用是否随本册待产品确认）+ 应用运行期凭据自动签发 / 回收（F049 底座）+ CLI `deploy` / `logs` 接线 + PRD-2 最小面 **WB-14 发布面 / WB-15 版本与差异** + 元信息随 deploy 更新。**体量大，spec 阶段可再拆**（审批与版本 vs 能力总线注入） | F112, F104（审批） |
| F056-app-square-governance | B | F054, F055 | RT-02 应用广场（与工作流 / 助手并列、共用标签体系、按可见范围过滤；标签设置本版不做）+ GOV-01 授权交互（卡片 ⚙️「管理权限」与发布面可见范围区两个入口拉同一弹窗、首发后「仅 owner 可见」显著提示）+ GOV-04 全链路审计（新增事件类型四处 lockstep、查询面「对象应用」筛选、超管租户筛选、导出）+ GOV-07 权限控制（`create_app` 既有权限点复用验收；卡片 ⚙️ 菜单按类型裁剪的**回归验证**——裁剪实现归 F054 卡片）+ §3.0.3 事件触达接线（站内消息） | F104 |
| F059-k8s-runtime-backend | B | F054 | GOV-10 k8s 集群形态：Deployment / Service reconcile、命名空间级 RBAC、档位 → Pod requests / limits、RuntimeClass 沙箱档位、NetworkPolicy 出站白名单、**镜像构建与分发**（无守护进程构建器 + 仓库对接，多节点必需件）、集群能力探测（CNI NetworkPolicy / StorageClass / RuntimeClass）与不满足时显式告警、双形态一致性回归（INV-33）、Helm chart / manifests 交付物。方案 F113「不可裁剪顺延」 | F113 |

**未映射 / 不映射项登记**：
- GOV-05 **密钥引用**：录入面 WB-05 属 PRD-2——**已定（Discovery N4，2026-08-17）**：本册能力总线只做模型 + 知识库两项，密钥引用整体随 PRD-2；能力声明中出现密钥引用时预检拒绝（F055 AC-56）。
- **DEV-06**（改码权交接）、**GOV-09**（管理后台应用列表）已取消；**RT-06 / GOV-06** 属 PRD-2；**RT-09**（iframe 嵌入升级，方案 F109）顺延、可随任意平台版本交付。
- DEV-07 验收 3（与平台内模板同范式）依赖 PRD-2 WB-01，随 PRD-2 验收。

---

## 已分配模块编码（MMMEE）

> 新 Feature 分配错误码时，必须检查此表避免冲突（存量分配见 v2.6.0 release-contract 与 `common/errcode/`）。

| 模块编码 (MMM) | 模块 | Owner Feature |
|----------------|------|---------------|
| 260 | open_api（开放 API 鉴权，26001–**26017** 见伴生 PRD 附录 C；`26013` / `26014` 已随决议移除、编号不复用；**`26017`「任务模式未开放」由伴生 D11 新增，与 `26015`「异步未开放」分属运行模式 / 传输方式两个维度，不得合并；承载这两个语义的具体字段归 design**）。`docs/constitution.md` C5 模块登记表明确「260 is reserved for the Open API (`/api/v2`) authentication module，尚未实现，不得他用」——伴生 PRD D5 已关闭 | F049（委托类错误码 26004–26007 / 26016 的实际启用随 F050）；落码时按 C5 要求在同一次改动中回写 constitution 表 |
| **161** | app_factory · **F054 段**（托管应用领域模型与运行时，`common/errcode/app_factory.py`）。子段：`16100-16119` 领域/状态机 · `16120-16139` 运行时/编排 · `16140-16159` 入口与身份注入 · `16160-16179` 数据面/日志 · `16180-16199` 部署开关/运维。本期启用 16101–16105 / 16121–16125 / 16141–16146 / 16161 / 16181（`16162` 数据 tab 随后置 Wave 落） | F054（已落码 2026-08-17，同批回写 `docs/constitution.md` C5 + 三语 `packages/locales/src/api_errors/`） |
| **162** | app_factory · **F055 段**（发布管线：预检 / 审批 / 版本冻结 / 档位实体） | F055（落码时新建 `common/errcode/` 文件，勿写进 161 那个文件） |
| **163** | app_factory · **F056 段**（应用广场 / 授权交互 / 审计查询面 / GOV-07） | F056 |
| **164** | app_factory · **F059 段**（k8s 运行时后端 / 镜像构建与分发） | F059 |

---

## 变更历史

| 日期 | 变更内容 | 影响范围 |
|------|---------|---------|
| 2026-08-06 | 初始化 v3.0.0 契约：登记编号约定（F049 起 / INV-27 起）、PRD-1 九个 Feature 的规划依赖图（表 3）、领域对象候选（表 1，未生效）；正式登记随各 spec 评审进行 | 全部 |
| 2026-08-06 | 对抗校验修正：GOV-05 主体归属 F055（原漏映射）；会话 key 派生归 F049、接线随 PRD-2；F049 范围收为 R1–R7+R9（R8 P2 顺延）；ResourceTier 由 F056 移至 F055 并把实例配额执行闸门划入 F054（消除依赖倒挂与无护栏窗口）；PRD-2 最小承载面逐面指名；beta1 契约来源分支与 INV-8~26 条件生效、历史编号空间说明补齐 | 表 1/表 3/编号约定 |
| 2026-08-06 | **开发基线定为 `3.0-vibe`（基于 `feat/3.0.0-beta1`，F048 已实装）**——Discovery 决策 D2 由分支选择确定；INV-8~26 因而无条件生效。**F049 按伴生 PRD 自身 P0/P1 阶段线拆分**：F049 收窄为 P0 鉴权底座（spec 定稿），R2/R3 委托与 R7 双归属并入 F050（更名 identity-modes-and-personal-key）。表 1 正式登记 ApiCredential 与 ServiceAccount 两个领域对象（原为候选）；表 2 登记 INV-27~30（默认拒绝 / 凭据底座 / 服务账号不可登录 / 开放面 fail-closed 加强） | F049、F050、表 1/表 2/表 3 |
| 2026-08-06 | 分支跟进 `feat/3.0.0-beta1`（rebase 至 `f4b85fa5e`）。据其新增的 constitution C5 模块登记表确认 **260 段已被明确预留给开放 API 鉴权模块且尚未实现** → 伴生 PRD 待决项 D5 关闭，错误码表相应改为「已确认可用」，并登记落码时须按 C5 回写 constitution | 错误码表 / F049 |
| 2026-08-06 | 对齐伴生 PRD 深度 Review 终版（`9bb8e89cc`）：该修订**整条否决开放 API 侧的个人访问令牌**（密钥只签发给服务账号，R1 全部归 P0，P1 估算降至 12–14 人天）。相应更新：F049 spec 排除项改写、新增 §4 待澄清-4；表 1 ApiCredential 去掉 `bs-pat-` 既定形态；INV-28 主体清单同步；表 3 F050 行范围与依赖注记更新 | F049、F050、表 1/表 2/表 3 |
| 2026-08-06 | **F049 spec §4 四项待澄清全部拍板（产品逐项确认），spec ★ 已过**。决议-4 为重大翻案：**个人 API key（`bs-pat-`）整条取消，全平台只有服务账号一类密钥主体**——PRD-1 修订至 v1.6。连锁：F050 更名 **identity-modes**；F051/F052 依赖由 F050 放宽为 F049；表 1 ApiCredential 与 INV-28 收敛为单一主体轨。决议-1~3 详见 F049 spec §4 | F049–F053、PRD-1、表 1/表 2/表 3 |
| 2026-08-15 | F049 spec 按伴生 PRD v2.0 对齐（决议-5）：兼容窗口废止、铁律 2 形态、服务账号不进选人场景 / 主体侧授权唯一入口、密钥级白名单不预留、`enable_guest_access` 移除；AC 40 → 47 | F049 |
| **2026-08-17** | **按 PRD-1 v2.0 + 伴生 PRD v2.1 重做 spec 层地基（拆分 v2）。** ① **表 3 重写为 11 个 Feature**：F050 收回「DEV-01 残余管理面 / GOV-07 flag」改承接 `delegate` 位与委托配置区（含 AC-48/46）；F051 去 Anthropic 面与聚合账单（改逐条审计）；F052 明确统一检索门面（伴生 §4.4.6 交付前提）；F053 由四包改两包、CLI 收五命令、加接入信息区与本地 sqlite；F057 五件套改三件套；F054 去实例配额执行闸门（租户实例配额已砍）、加 compose 编排后端 / app-proxy / GOV-10 层开关 / 详情页壳（WB-13 / WB-06 改落 platform「构建 → 应用」）；F055 去应用 token 产品概念 / DEV-06 / 平台侧回滚 / 变更摘要，改 GOV-02 **平台预置审批流 + 三项阻塞前置**、GOV-03 待上线态成因泛化为「资源不足」；F056 去 GOV-09 与租户实例配额 UI、标签设置，加 GOV-07 `create_app` 复用与 ⚙️ 菜单裁剪；**新增 F058 openapi-responses（登记项）与 F059 k8s-runtime-backend（GOV-10 k8s 形态 + 镜像构建分发）**。② **表 1**：ApiCredential 补应用运行期凭据由 F055 编排 / 会话派生凭据移出、ServiceAccount 补资源归属人与「不得授予管理员身份」；撤销 AppServiceAccountToken 候选；App/AppVersion/AppInstance 候选加状态机与快照四要素、ResourceTier 候选加三档 / 超管可调、新增「应用发布」审批场景预置候选。③ **表 2**：INV-27 补「无兼容窗口 / 6 端点靠关闭」；INV-28 补应用运行期凭据；**INV-29 修正**（不进任何选人场景 / 主体侧唯一入口 / 不得授予管理员身份，原 opt-in 口径作废）；**新登记 INV-31**（三扩展位仅模式 S 且与 `delegate` 互斥）；候选 INV-32~36（平台注入身份唯一可信 / 双形态一致 / 发布必审 / 资产持久显式删除 / 托管期检索两层）。④ 错误码表 app_factory 段位注 161 起。⑤ F049 spec 整体重写：转型降权闸口与会话反馈宽松档作废、6 个 `/chat/*` 不暴露、资源归属人 AC-23–28、授权页单列表 + 来源列 + 全部撤销排除回授、删除不阻断、`default_operator` 移除、三扩展位随开放能力层出现、本期不提供 `delegate` 位、开放能力层部署开关归 F049、ShareLink 写增量登记；AC 47 → 65（决议-6，含四项 [待澄清] f/g/h/i；同日经 `/sdd-review` 独立审查 24 条发现修订） | 全部 |
| **2026-08-17（全自动模式）** | 用户授权全部按建议拍板：N1–N6 与 F049 决议-6 f/g/h/i 定案；两 PRD 残留回修。**spec 层齐备 8/11**：F053（55 有效 AC）/ F054（65）/ F055（65）/ F056（45）/ F051（33）/ F052（47）新写并经独立审查修订（F050 / F057 / F058 / F059 进行中）。**表 1 转正**：App / AppVersion / AppInstance（F054）、ResourceTier / 「应用发布」审批场景预置 / **AppManifest**（F055）；**表 2 转正 INV-32~36**（INV-36 补审批期例外）。**跨 Feature 裁定**：审计事件写入归动作 owner、F056 只拥有登记 + 查询面 + 验收；触达同理；卡片 ⚙️ 裁剪实现归 F054；发布面可见范围区归 F056、F055 留槽位；应用运行期凭据主体 = 「托管应用」类型（F049 底座第二类主体）；F054 五个状态动作是应用态唯一写入方；MCP 应用数据工具读写不含 DDL、服务端接口归 F054 AC-56；`bisheng-app.yaml` 形态归 F055。新增 `mvp-114-path.md` 纵切说明；ui-demo/ 交互稿提取 | 全部 |
| **2026-08-17（伴生 PRD v2.2）** | 用户裁定两项，落 §4.6.3 并下传 F058 spec：① **D11 — 日常/任务模式的对外区分改用 `bisheng_mode` 扩展字段，`background` 恢复 OpenAI 原义**，推翻原「`task_mode` ⟷ `background` 一一对应」；能力轴与传输轴解耦、各自留位、各自拒绝；**错误码 260 段新增 `26017`**（与 `26015` 不得合并）；伴生新增 AC-50、F058 新增 AC-37 与决议-13。取证：内部两个分支 `_task_mode_stream_completion` / `_agent_stream_chat_completion` 都是同步 SSE 流（`chat_service.py:2148-2150`），旧映射连现状都不成立。② **D12 — Chat Completions 薄兼容层由「待客户场景决定」改判为明确不做**，进伴生 §六非目标、附录 B.1 对应行划除；F058 决议-2 相应升格 | F058；错误码表 260 段 |
| **2026-08-18（伴生 PRD v2.3）** | 研发反馈「强行对标 OpenAI Responses 工作量大、且逼调用方传 system message 等模型层入参」，用户裁定：**PRD / spec 只写业务需求，对外接口形态交研发定技术方案**（伴生 **D9 改判**，展开见伴生 §7.5）。① 伴生 §4.6.3 整节重写为「业务入参清单 + 八条与形态无关的硬边界」，原 Responses 字段映射 / 事件对应 / 拒绝清单全部移出；② **连带取消「官方 OpenAI SDK 可直连日常模式会话」这一产品需求**（伴生 AC-36 改写、进 §六非目标）——该诉求由 F051 `model:invoke` 面与既有助手端点承接；③ D11 / D12 规则不变、表述泛化（D11 只约束两个维度不合并、不指定承载字段；D12 泛化为「同一能力不做第二套并行契约」）；④ F058 spec 全部 AC 去形态化、AC-04 由「SDK 反序列化」改为「通用 HTTP 客户端 + 可运行示例」、AC-02 多轮机制由「上一轮 id」改为**会话标识**（「指向历史轮次 → 400」的分叉判定随之消失）、新增决议-14；⑤ **F058 估算须重估**（砍掉对象模型保真 / 事件类型对齐 / SDK 验收 / 完整拒绝清单一批纯兼容性工作量） | F058；伴生 PRD 全册 |
