# 复制即跑（只看这一份）

不要看 README 里的表去想下一步。  
打开本文件，从上到下，**一整段复制 → 粘贴回车**。某步报 `FAIL` 就停，不要跳。

- 在哪跑：SSH 进 **B 演练机** `10.168.24.121`（端口 `52012`）
- 生产 B 不要用这份（里面 `DRILL=1`）
- 回滚：`p2/90-rollback.md`

---

## 0. 笔记本上：把整个仓库压缩包拷到 B

2.5 hop 会用压缩包里的 `docker/` **整份覆盖**现场 compose / config.yaml / entrypoint / nginx，只拷升级脚本不够。

```bash
cd /Users/lkk/workplace
tar -czf bisheng-2.5.0-sg.tgz \
  --exclude bisheng/.git \
  --exclude bisheng/src/frontend/*/node_modules \
  --exclude bisheng/docker/mysql/data \
  --exclude bisheng/docker/data \
  bisheng
scp -P 52012 bisheng-2.5.0-sg.tgz root@10.168.24.121:/data/
```

然后 SSH：

```bash
ssh -p 52012 root@10.168.24.121
cd /data
tar -xzf bisheng-2.5.0-sg.tgz
# 升级脚本在解压后的仓库里
cd /data/bisheng/scripts/upgrade-ab-fusion
```

找不到 `docker/docker-compose.yml` 时，显式指定：`export PACK_DOCKER_DIR=/data/bisheng/docker`。

---

## 1. B 上：开门禁

compose 路径、project 名、config.yaml / entrypoint.sh 的宿主机位置**全部自动发现**（从运行中的容器标签和挂载表反查，见 `lib/discover.sh`），不用填 `env.sh`，换机器也不用改脚本。

但门禁默认全关，必须显式打开，否则脚本只打印不落库：

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
export APPLY=1          # 允许真正 ALTER / 换镜像
export CONFIRM_LAYOUT=1 # 已核对过脚本打印的「部署布局发现结果」
export DRILL=1          # 演练机专用：跳过 digest 硬门禁。生产禁止设置
```

镜像和容器名也都可以用环境变量覆盖，例如 `export BACKEND_CONTAINER=xxx`。每个 hop 先 SQL、再改 compose、再 `compose up`（自己拉镜像），不用单独 `docker pull` / `set-images.sh`。

`p2/30-hop-2.5.sh` 已钉 D02 digest。B 上若没有该镜像，先 `docker load`，不要改成浮动 tag。

---

## 2. 预检（不改数据）

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
bash p2/00-precheck.sh
```

看到 `预检通过` 再往下。若大量列已经是 `PRESENT`，说明这库升级过，不要继续 hop。

---

## 3. 停服务 + 备份 MySQL

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
bash p2/01-freeze-and-backup.sh
```

---

## 4. 升到 2.3-beta1（含 convert_all）

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
bash p2/10-hop-2.3-beta1.sh
```

---

## 5. 升到 2.3

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
bash p2/11-hop-2.3-release.sh
```

---

## 6. 升到 2.4-beta1

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
bash p2/20-hop-2.4-beta1.sh
```

---

## 7. 升到 2.4

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
bash p2/21-hop-2.4.sh
```

---

## 8. 个人知识库 type=2

演练机已盘过：只有 `id=17` 且有文件，按评审默认转换。

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
CONFIRM_TYPE2=1 bash p2/22-hop-2.4-type2.sh
```

---

## 9. 升到 2.5.0-sg

从 **已是 2.4.0** 升上来。`p2/30-hop-2.5.sh` 会自己补 OpenFGA、换 D02 镜像、两段 Alembic。不要再单独跑 `ensure-openfga.sh`。

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
bash p2/30-hop-2.5.sh
```

`alembic current` 必须等于 `f058_dashboard_dataset_flags`。失败即停。不要 `alembic stamp head`。

---

## 10. 权限 F006 + 工作台模型

先看预览：

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
CONFIRM_F006=0 bash p2/31-f006-workstation.sh
```

没有报错再落库：

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
CONFIRM_F006=1 bash p2/31-f006-workstation.sh
```

---

## 11. 计数验收

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
bash p2/40-verify.sh
```

然后自己点：登录、开一条工作流、打开一个传统知识库。  
**不要打开首钢同步。**

---

## 12. P4 身份（B 必须已是 2.5.0-sg；默认不落库）

先导出，再生成候选 CSV，**人工签字**后才 APPLY。禁止按姓名自动合并。演练机 B 会对不上 A 的员工编码，大量 `create_on_b` 是预期。

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
# 会话里需要时: export SSHPASS='...'   # 不要写入仓库
bash p4/00-export-a-users.sh
python3 p4/01-propose-map.py logs/p4/a-users.csv logs/p4/b-users.csv --out-dir logs/p4
# 复核 logs/p4/user-map.proposed.csv / conflicts.tsv / manual.tsv
# 签字后:
cp logs/p4/user-map.proposed.csv p4/user-map.csv
APPLY=0 bash p4/apply-takeover.sh
# 复核 logs/p4-takeover-*.sql 后再:
# APPLY=1 bash p4/apply-takeover.sh
```

`APPLY=1` 前：`user.source` 存在且 `alembic_version=f058_dashboard_dataset_flags`。不要打开首钢同步。

---

## 13. P5 知识空间（先单空间；默认 APPLY=0）

B **不要**开共享存储。只拷 MinIO 原文件，在 B 按每空间私有索引重建。

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
# 在 logs/p5/batch.txt 写 A 的 space_id，或直接 --space-id
mkdir -p logs/p5
echo '123' > logs/p5/batch.txt   # 换成真实 A 空间 id
bash p5/00-export-a-space.sh --space-id 123
bash p5/10-inventory.sh
bash p5/20-dry-run.sh            # 所有者映不上会停
APPLY=0 bash p5/30-apply.sh --space-id 123
# 确认 dry-run 与将拷对象数后再单空间落库:
# APPLY=1 bash p5/30-apply.sh --space-id 123
bash p5/40-verify.sh
# 失败只回滚本批新资源:
# APPLY=0 bash p5/90-rollback.sh logs/p5/rollback-manifest-*.json
```

入队成功 ≠ 解析完成。Worker/Embedding 未就绪时 verify 会看到非 SUCCESS，不要发布。

---

## 还没到（先不要跑）

- P6 切流量：`p6/cutover-runbook.md`
- 积分：D11b 已改为映射后迁账户+流水，**脚本未写**；先 `bash p1/run-inventory.sh` 看 `POINTS_*`。`point_rule` 另定。
- 收藏/订阅（D11a，映射后另脚本）、会话（D10 不迁）、A 传统库 type=0、打开共享存储 / 首钢同步
