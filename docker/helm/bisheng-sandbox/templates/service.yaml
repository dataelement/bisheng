apiVersion: v1
kind: Service
metadata:
  name: {{ include "bisheng-sandbox.fullname" . }}
  labels:
    {{- include "bisheng-sandbox.labels" . | nindent 4 }}
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    {{- include "bisheng-sandbox.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
