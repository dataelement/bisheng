# P1 只读盘点报告

基线：`毕昇A-B环境升级融合方案-客户评审版.md` 第 4.2 / 5.3 / 6 / 7 节  
采集时间：2026-09-09  
范围：本次给出的两台主机（只读，未改库、未改配置）

> 密码未写入本目录任何文件。`tosql` 账号只能看到 B 上的 `tosql` 库，不是毕昇库。

## 0. 先说清楚：这两台不像生产 A/B

**客观事实**

| | 本次主机 | 镜像 / 角色 |
|---|---|---|
| A | `10.171.0.50:52012` Oper1 | `bisheng-backend-shougang:v2.5.0-amd64-test`，同机还有 portal `v1.0-test` |
| B | `10.168.24.121:52012` root | `bisheng-*:v2.2sgv260402-JiTuan`，你标注为「场内 2.2 **测试**环境」 |

**逻辑推演：** 盘点结果只代表这两台。评审稿里的生产 A（知识空间+组织同步）和生产 B（2.2 工作流+传统知识库）如果另有机器，不能用这份数去做 P3/P5。

**待你确认：** A 的 `10.171.0.50` 是不是要融合的那套 2.5.0-sg 数据源；B 的 `10.168.24.121` 是只做升级演练，还是就是最终要升的那套 2.2。

---

## 1. 运行水位（D02 部分可填）

### A（2.5.0-sg）

- Alembic：`f058_dashboard_dataset_flags`（本地 `feat/2.5.0-sg` 有同名 revision）
- Backend 镜像：`10.171.0.14:21020/skm-images/bisheng-backend-shougang:v2.5.0-amd64-test`
  - image id `sha256:323841788ac117d81681d1b148114bd262d96f17cc5d1a7d6fbe54247ff341da`
  - digest `sha256:205a3c29fbecc78fad6ab0f0c641e0dc18127f4d9776a8608cc00e768537bc18`
  - Created 2026-09-06
- Frontend digest `sha256:f95cebbb44e6cb49d8cfc90be1343a8532d8ce2224481cc1ba78c525d74f7ae9`（Created 2026-09-03，与 backend 不是同一天）
- 镜像内无 Git commit label → **D02 的 commit 仍缺**，不能假装已冻结目标包

### B（非官方 2.2.0-release）

- **无** `alembic_version`（符合 2.2 `create_all`）
- Backend：`harbor.shougang.com.cn/llmplat/bisheng-backend:v2.2sgv260402-JiTuan`
  - digest `sha256:eb3cd82f4961c74549ec4d4ead7845f679dd68b57e82a604b0c7cfd7172f5ead`
  - 镜像 Created 2026-04-02；容器 Created 2026-05-10
- Frontend：`...:v2.2sgv260402-JiTuan-bak` digest `sha256:f7d216cd0acc9eda40f4f2031ffabc9c5db55be5c3be4dbd4c597e81c4b42a26`
- `user` 有定制列 `user_name_cn`、`dept_id`（字符串）
- Worker 容器 health=unhealthy：healthcheck 去打本容器 `localhost:7860`（Celery worker 不听 7860）。backend 本体 healthy。这是探测写错，还不能当成业务挂了。

**主观判断：** 官方「2.2 → 2.3-beta1 SQL → 2.4 知识空间 SQL」 hop **不能直接套** 这套 `v2.2sgv260402-JiTuan`。P2 演练前要先 diff 这镜像和官方 2.2 的表结构。

涉及表：`user` `knowledge` `knowledgefile` `flow` `flowversion` `assistant` `message_session` `chatmessage` `role` `userrole` `group` `usergroup` `t_gpts_tools`；A 另有 `alembic_version` `department` `space_channel_member` `failed_tuple` `knowledge_document` `org_sync_config` `tenant`。本次无 DDL。

---

## 2. 四份清单（评审稿 4.2）

### 2.1 用户映射（几乎对不上）

| | A | B |
|---|---|---|
| 用户总数 / 未删除 | 21470 / 17442 | 23 / 23 |
| source | local 11060，sg 10407，sso 3 | 无 `source`/`external_id` |
| 邮箱/手机 | 有列 | **全部 NULL** |
| user_name 唯一 | 否（dup 组 8707，大量中文重名） | 是，无重复 |
| 员工编码重复 | **7579 组，全部是 `local+sg` 各一行，共 15158 人**；组内几乎都是双活（15150 active / 8 deleted） | 无该列 |

B 的 23 个登录名拿到 A 去撞：大小写不敏感下只有 `test11` ↔ A `Test11`（local，external_id=`Test11`）。`qslab` / `wangdan` / `xumeng` 等其余 22 个在 A **不存在**。

样例（A 双行，同一员工编码）：

| user_id | user_name | source | external_id |
|---|---|---|---|
| 5945 | 艾敬乐 | local | aijl0026 |
| 18320 | 艾敬乐 | sg | aijl0026 |

缺 `external_id` 的未删除用户：仅 `user_id=11720 admin`（local）。

**逻辑推演：** 按评审稿「同员工编码接管、禁止按名合并」，在这两套数据上 **B→A 自动映射数为 0**。A 侧 local/sg 双 `user_id` 迁知识空间时必须先定保留哪条，否则成员/所有者会裂成两个人。

### 2.2 部门映射

- B：无 `department` 表；`user.dept_id` 全 NULL。
- A：部门 3747（active 3365，source=sg 1669）；部门 `external_id` **无重复**。
- 租户：仅 `id=1 default / Default Tenant`。
- 组织同步：`org_sync_config` 一条 `sso_realtime / SSO Gateway (internal)`，`sync_status=disabled`。

P4 身份接管在这套 B 上没有现成部门可对。

### 2.3 资源 / ID 映射

**A 知识空间（type=3）** 225 个，空间内文件 1785；另有传统库 type=0 共 2 个（`测试`、`测试1`）。  
文件状态：SUCCESS(2)=1079，FAILED(3)=622，VIOLATION(7)=86，TIMEOUT(6)=1。无 PROCESSING/WAITING。  
文档版本链：`knowledge_document` 1431，`knowledge_document_version` 1434。  
成员：CREATOR 228 / ADMIN 48 / MEMBER 25。  
OpenFGA `failed_tuple`：23876 行全是 `succeeded`。  
A 工作流 2、助手 0、工具 37。会话 489 / 消息 1005（评审稿默认不迁）。

明细：`A-spaces.tsv`。

文件数前 5：科室库-潘娜 723；cwznadmin的知识库 301；行业情报 173；政策制度 68；炼铁作业部库 58。

**B 传统知识库** type0=41、QA type1=5、个人 type2=1（评审稿 5.3：`id=17 个人知识库` user_id=1，2 个文件，非空）。文件共 147。无孤儿资源。  
工作流 95（flow_type 10=81，type 1=14）、版本 164、助手 45、工具 149。会话 550 / 消息 3981。

明细：`B-knowledge.tsv`。

ID 策略：A 知识空间 id 从约 3469 起；B knowledge id 最大几十。融合时仍须 **B 重新分配 ID + 映射表**，不能原样插入 A 的 id。

### 2.4 冲突 / 客户决策

1. **环境是不是生产 A/B** — 未确认则后面阶段全部作废。
2. **B 不是官方 2.2.0-release**，是 `v2.2sgv260402-JiTuan`。官方 hop SQL 要先做表 diff。
3. **A 员工编码双活（local+sg）** — 评审稿写 B 上重复编码是 P0。A 上这是同步产物还是要先合并，需要产品拍板后再做 P4/P5。
4. **B 用户无邮箱手机、与 A 登录名几乎无交集** — 身份映射只能人工对照花名册；不能按名合并。
5. **A 622 个 FAILED + 86 VIOLATION** — 迁空间前要定：失败文件带不带、违规文件带不带。
6. **A frontend/backend 镜像日期不一致** — D02 目标包未冻结。
7. **D02 Git commit 缺失**。
8. 同名知识库：两边都有大量「测试」类名字，按评审稿 **不合并、记冲突**，迁到 B 后用新 ID。
9. B worker unhealthy 探测方式错误 — 演练前改掉或忽略，避免误判。

---

## 3. 按评审稿现在能不能往下

| 阶段 | 状态 |
|---|---|
| P0 D02 镜像 digest | 这两台已采到；**目标 2.5.0-sg 生产包 commit 仍缺** |
| P0 D03–D19 | 未签字 |
| P1 四清单 | 这两台已出初稿；**用户映射为空，等你确认是否盘错环境** |
| P2 升级演练 / 融合工具 | **不做**。评审稿：盘点未齐、待确认项未补齐前不正式执行 |

原始输出在 `scripts/upgrade-ab-fusion/p1/raw/`（已 gitignore，勿提交）：`A-inventory.txt`（含全量用户，勿外传）、`B-inventory.txt`、`A-summary.txt`、`A-rest.txt`。
