# 测试1-1 积分规则 Playwright 高标准验收报告

| 项 | 内容 |
|----|------|
| 时间 | 2026-08-08 18:51–18:54（CST） |
| 组织 | 测试1-1（dept_id=7，业务 ID `BS@5413b4`） |
| 入口 | Portal `http://127.0.0.1:5173`；Backend `:7860`；BFF `:8010` |
| 套件 | [`gates/accept_test11.spec.ts`](../gates/accept_test11.spec.ts) |
| 原始结果 | `/tmp/accept_test11.log`（Playwright JSON）；截图/trace 见 `test-results/` |
| 总体判定 | **有条件通过** |

## 1. 结论摘要

- **G1–G4 发分、R1–R3 管理端扣减、M1/M4/M6 管理端规则展示**：通过（覆盖 4 名组织用户 + admin）。
- **月度引擎实发（M*）**：环境阻塞（ES 缺 `mid_user_daily_participation_fact`），记 **BLOCKED**，不判定产品逻辑失败。
- **前台「积分规则」弹窗文案**：隐藏 M* 通过；「申诉 / 线下」关键字 **FAIL**（公开 guide 未露出期望文案）。

主链路可验收上线；文案缺口与月度登录事实依赖需单独跟进。

## 2. 环境与账号

| 服务 | 端口 | 状态 |
|------|------|------|
| bisheng-backend | 7860 | up |
| portal-bff | 8010 | up |
| portal-web | 5173 | up |
| bisheng-client | 4001 | up |
| bisheng-platform | 3001 | up |
| Celery `points_award_local` | — | 进程存在（Gate 强制 `POINTS_GATE_FORCE_SYNC=1`） |

| 账号 | user_id | 角色 |
|------|---------|------|
| admin | 1 | 平台超管（规则配置 / 扣减 / M Tab） |
| gzx0022 | 33 | 测试1-1；R1 被扣；我的积分 |
| gzx0023 | 34 | 测试1-1；G1 publisher 受益 / G3 / R2 |
| gzx0024 | 35 | 测试1-1；G4 / R3 |
| gzx0025 | 36 | 测试1-1；G2 + 幂等 |

密码：`E2E_POINTS_PASSWORD`（未写入本报告）。

## 3. 规则启用快照

`accept_test11_trigger.py rules_snapshot`：

| code | status | name | 备注 |
|------|--------|------|------|
| G1 | enabled | 发布/上传到公共库 | fixed 3 |
| G2 | enabled | 发布/上传到部门库 | fixed 2 |
| G3 | enabled | 文档被收藏 | tier + lifetime_cap 15 |
| G4 | enabled | 问答被采纳 | fixed 3 |
| R1 | enabled | 色情低俗/暴力违法 | -100 |
| R2 | enabled | 不当言论 | -100 |
| R3 | enabled | 其他违反规范 | -100 |
| M1 | enabled | 公共库所有者月奖 | 200 |
| M4 | enabled | 部门管理员月奖 | 100 |
| M6 | enabled | 科室管理员月奖 | 50 |

未纳入本次范围：G5/G6（disabled）、G7（enabled 但不在矩阵）、M2/M3/M5/M7/M8（disabled）。

## 4. 用例结果矩阵

| ID | 场景 | 用户 | 结果 | 证据要点 |
|----|------|------|------|----------|
| P0 | 目标规则全部 enabled | — | **PASS** | rules_snapshot ok |
| G1 | beneficiary=publisher，发分落 publisher | admin UI + gzx0022/23 | **PASS** | logs.user_id=34；gzx0023 我的积分可见 |
| G2 | 部门库上传发分 + 同 file_id 幂等 | gzx0025 | **PASS** | count/balance 重放不变；Portal 明细可见 |
| G2- | 超管上传不发 G2 | admin | **PASS** | skipped=true |
| G3 | 收藏阶梯发分 | gzx0023 | **PASS** | factory award_g3；Portal 明细 |
| G4 | 问答采纳发分 | gzx0024 | **PASS** | factory award_g4；Portal 明细 |
| R1 | UI 违规扣减 | gzx0022 | **PASS** | 按用户名精确选行；latest_deduct 负向 |
| R2 | UI 违规扣减 | gzx0023 | **PASS** | 同上 |
| R3 | UI 违规扣减 | gzx0024 | **PASS** | 同上；操作记录见 R1/R2/R3 |
| M-UI | 管理员奖励 Tab 展示 M1/M4/M6 启用 | admin | **PASS** | 行内「启用」 |
| M-ENG | 月度引擎 dry-run | tenant=1 | **BLOCKED** | awarded=0 skipped=10 error=`login_query_failed`；缺 ES index `mid_user_daily_participation_fact`；period=`2026-07` |
| X1 | ≥3 用户打开我的积分 | 22/23/24 | **PASS** | `/points` 摘要+明细 |
| C1 | 规则弹窗隐藏 M* + 申诉/线下 | gzx0022 | **FAIL** | 隐藏 M* 已满足；`/申诉\|线下/` 不可见 |
| C2 | 规则弹窗「线下」关键字（strict） | gzx0022 | **FAIL** | 单独复跑确认；无「线下」 |

套件统计（主跑）：expected=10，unexpected=1，skipped=1（serial 在 C1 失败后跳过 C2；C2 已单独复跑确认 FAIL）。

## 5. 失败 / 阻塞根因

### 5.1 C1/C2 规则弹窗文案（产品缺口）

- 期望：公开规则说明含申诉指引，高标准含「线下」关键字（对齐历史 GM4）。
- 实际：弹窗可开、不展示月度 M* 名称，但 DOM 中无「申诉」「线下」可见文本（guide 可能未下发/未渲染到可访问文本）。
- 附件：  
  `test-results/accept_test11-accept-test1-6c496-des-M-and-shows-appeal-copy-points-gates/`  
  `test-results/accept_test11-accept-test1-6903f-line-appeal-keyword-strict--points-gates/`

### 5.2 M* 月末实发（环境阻塞）

- `PointsMonthlyRewardService.run_for_tenant(1)` → `login_query_failed`。
- ES：`index_not_found_exception` for `mid_user_daily_participation_fact`。
- 规则启用与管理端展示已验；**实发需补齐统计 ES 索引后再验**。

## 6. 相对历史 Gate 的改善

| 历史问题 | 本次 |
|----------|------|
| GM2 幂等 balance 漂移 | accept_test11 G2 在 `POINTS_GATE_FORCE_SYNC=1` 下 PASS |
| GM4 UI 扣减 latest_deduct 找不到（疑似误点行） | 按 **user_name + user_id** 精确选行后 R1–R3 全 PASS |
| 「线下」文案 | 仍 FAIL（未修产品文案） |

## 7. 建议跟进（不在本次改代码范围）

1. 核对公开积分 guide（DB `point_copy` / 管理端「文案」）是否含申诉说明，并决定是否必须出现「线下」。
2. 补齐/配置统计 ES 索引 `mid_user_daily_participation_fact` 后重跑 M* 实发。
3. 需要全绿发布门禁时，将 C1/C2 与 M-ENG 解阻后再跑 `npm run test:accept11`。

## 8. 复现命令

```bash
cd src/backend/test/e2e_ui/points
export E2E_POINTS_RUN_GATES=1 \
  E2E_PORTAL_BASE_URL=http://127.0.0.1:5173 \
  E2E_POINTS_PASSWORD='…' \
  E2E_POINTS_ADMIN=admin \
  POINTS_GATE_FORCE_SYNC=1
npm run test:accept11
```

---

**签署口径**：业务主路径（G1–G4 / R1–R3 / M 规则配置可见）在「测试1-1」多用户场景下验收通过；公开规则申诉文案与月度实发环境未达高标准全绿 → **有条件通过**。
