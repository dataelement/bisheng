# F053 实施记录

用户已在会话中确认需求与设计，并授权修复、提交和推送；环境发布不在本次授权范围。

- [x] T1：确认指定 tag/分支/worktree；依据既有组织投影协议和模型检查部门事实来源。
- [x] T2：组织仓储按用户及祖先 ID 有界读取，提供完整、经过状态检查的事实端口；保持权限领域不读取组织表。
- [x] T3：模型 subtree_member 改为直接 user 关系；统一注入四类 FGA 查询，API/Celery/Linsight 装配，操作内复用与持久缓存隔离。
- [x] T4：禁止运行时永久写入临时关系；模型发布脚本增加历史 tuple 扫描，保留原离线切换门禁。
- [x] T5：模型语义、完整组织链/挂载/移除/迁移/异常、输入上限和运行时装配回归。
- [x] T6：原版 OpenFGA HTTP + SQLite 组织仓储，4681 部门与 40 层链验证，独立 Store 自动清理。
- [x] T7：Ruff、差异/架构检查及人工代码复核，记录基线既有测试失败和上线验证边界。
- [ ] 发布验证：MySQL/DM8、完整业务 API E2E、代表性并发与生产缓存配置；随环境部署另行执行。

## 实际偏差记录

没有改变用户确认的方案。细节补充见 design.md §2–4：保持既有跨租户挂载路径、只对依赖子树的关系加载组织事实、发布时审计永久 subtree_member，防止旧缓存和历史 Store 数据破坏请求事实语义。

架构脚本在绝对路径包含仓库目录名 `/bisheng/` 时，会把 backend/tests 与维护脚本误识别为业务模块（RULE-9）；使用仓库相对源码路径执行后无告警。未为了消除误报改变守卫或测试中的正常基础设施调用。
