# 客户决策确认单

对照：`毕昇A-B环境升级融合方案-客户评审版.md` 第 14 节。  
「我方建议」按 **A=`10.171.0.50`（迁出源）、B=`10.168.24.121`（2.2 演练机，不是最终生产）** 的 P1 盘点和当前演练填写。生产 B 另盘后，D04/D05/D07/D16 数字要复核，规则不必重开。

**关闭方式：** 2026-09-10 按我方建议全部采纳；客户结论默认 **同意**。非正式四方手签，客户事后可改单条。

| 编号 | 决策项 | 评审稿推荐值 | 已关闭结论 | 依据（事实 / 推演） | 客户结论 |
|---|---|---|---|---|---|
| D01 | 最终统一环境 | 升级后的 B | 最终生产 = 升级后的生产 B，不是这台演练机。A 迁完只读归档。 | 事实：已确认 `10.171.0.50` 是迁出源，`10.168.24.121` 只做升级演练。 | 同意 |
| D02 | 目标 2.5.0-sg 构建 | 固定提交和镜像摘要 | 演练包钉下面附表。生产切换前核对 A 现网是否同一制品，不是同一套就先对齐或出差异清单。 | 事实：A 现网前后端不是同一天的 test 镜像，无 Git commit。演练用本机 `feat/2.5.0-sg` 现打的包。 | 同意 |
| D03 | 用户权威标识 | 首钢员工编码 | 只用员工编码。禁止按姓名/用户名/手机/邮箱自动合并。 | 事实：A 登录名重复 8707 组；B 邮箱手机全空；B 登录名与 A 几乎对不上。 | 同意 |
| D04 | B 独有本地用户 | 保留，不自动删除 | 保留。是否再绑 SG 等花名册，升级时不删。 | 事实：演练 B 23 人，22 人在 A 不存在。 | 同意 |
| D05 | 无员工编码或歧义用户 | 人工逐项，不按姓名合并 | 演练不做自动映射；生产用花名册。映不上则其作为所有者的空间不发布。 | 事实：这两套自动映射 ≈ 0。A 缺编码未删用户仅 `admin`(11720)。 | 同意 |
| D06 | 部门权威体系 | 首钢外部部门编码 | 编码相同保留 B 部门 ID；同名不同编码不合并。演练机 B 无部门表，本阶段不接管部门。 | 事实：演练 B 无 `department`。A 部门 `external_id` 无重复。 | 同意 |
| D07 | B 旧 `type=2` 个人知识库 | 有文件则转换；空/无主不自动删 | 有文件按历史规则转换。演练机已转换 `id=17`。生产 B 另盘后同一规则。 | 事实：演练机非空 type=2 仅这一条。 | 同意 |
| D08 | A 知识空间版本历史 | 迁完整版本链 | 迁完整链，不改成只迁主版本。 | 事实：A 文档 1431、版本 1434，几乎 1:1。 | 同意 |
| D09 | 同名空间/文件 | 不自动合并；同内容去重，不同内容改名都留 | 不自动合并。迁入项用新 ID，必要时加后缀。 | 事实：两边大量「测试」类同名。 | 同意 |
| D10 | A 会话、审计、遥测、同步日志 | 不合并，A 只读归档 | 不迁入 B。 | 事实：A 会话 489 / 消息 1005；B 已有 550 / 3981。 | 同意 |
| D11a | A 收藏 / 置顶 / 订阅 | 映射后迁；映不上跳过 | 用户映射完成后再迁；映不上跳过并报告。实施前补数量统计。 | 推演：无映射会挂错账号。 | 同意 |
| D11b | A 积分 / 统计 | 单独确认 | **映射后迁账户+流水**（`user_point_account` / `user_point_log` 等用户账）。`point_rule` 用 A 覆盖还是保留 hop 种子 **另定**。另写脚本，不塞进 P5 知识空间。生产 B 切流量前关入口与积分 Beat；APPLY 前 B 用户流水应为 0（演练脏数据可清）。 | 原推演「升完 2.5 就有积分账」把演练机/升级窗口和 **生产正式运营** 混了。生产运营在 A 数据迁完之后才启动，A 的账就是生产账。必须按冻结的 `fusion_user_map` 改写 `user_id`，不拷 A 自增 ID。 | 同意（2026-09-10 改单：由「不迁」改为「迁流水」） |
| D12 | 分享链接、Token、密钥 | B 重签/重配，不复制 | 不复制。 | 安全与评审一致。 | 同意 |
| D13 | 共享存储切换 | 与本次融合解耦 | 本次不做。演练和首次融合都不要开共享存储。 | 推演：与 2.2→2.5 叠在一起回滚不可控。 | 同意 |
| D14 | 租户映射 | 盘点后写明 | A `tenant_id=1 default` → B `tenant_id=1`。禁止原样写入 A 的其它数字租户 ID。 | 事实：A 仅这一租户。生产 B 若已多租户则重盘。 | 同意 |
| D15 | RPO | 最终冻结增量后 RPO=0 | 生产切换 RPO=0。演练残差必须写进报告，不能当成生产可接受。 | 知识空间以空间为一致性单元。 | 同意 |
| D16 | 维护窗口 | 演练最大耗时 + 30% | 同意该公式。小时数待 2.5 演练后回填。升级窗口和融合切换拆成两次。 | 事实：2.5 hop 尚未跑完，没有实测停机时间。 | 同意 |
| D17 | A 只读观察期 | 建议 2–4 周 | **2 周。** | 判断：太短权限问题来不及暴露，太长双边运维成本高。 | 同意 |
| D18 | 快速回滚截止点 | 切换前共同确定 | 截止点 = 迁入空间对外发布、DNS/Gateway 切到 B 之前。过点且 B 已有新写入则只前向修复。 | 评审 11.2。 | 同意 |
| D19 | 可接受例外 | P0=0；P1 原则上 0；P2 清单签字 | P0=0。P1 允许 X02 违规文件、花名册对不上的非核心用户进签字清单。FAILED 文件不进「永久不迁」，走 X02。 | 评审 10.3 允许例外清单，禁止伪造成功率。 | 同意 |

---

## 评审稿未单列、盘点后必须拍板

| 编号 | 决策项 | 已关闭结论 | 依据 | 客户结论 |
|---|---|---|---|---|
| X01 | A 上同一员工编码同时有 local 一行和 sg 一行 | 两行都映到同一个 B `user_id`。B 上只留一个账号。不要在 A 上先删一行再迁。 | 事实：7579 组双活。例 `aijl0026` → `5945` local / `18320` sg。 | 同意 |
| X02 | A 解析失败 / 违规 / 超时文件 | SUCCESS 正常迁并在 B 重建索引。FAILED、TIMEOUT 迁原文件+元数据，在 B 重新解析。VIOLATION 不进检索、不当成功，进例外清单。 | 事实：SUCCESS 1079、FAILED 622、VIOLATION 86、TIMEOUT 1。 | 同意 |

---

## D02 演练目标构建（已冻结）

| 项 | 值 |
|---|---|
| Git commit | `89672f15a81c161cfa0a9f858d165a702159f362`（`feat/2.5.0-sg`，本机打镜像时的 HEAD） |
| backend | `dataelement/bisheng-backend:v2.5.0-sg` digest `sha256:5268c513f98c530d65b0ee04fb226cdde5dc4bc287bcb5583f789082ae99ee20` |
| frontend | `dataelement/bisheng-frontend:v2.5.0-sg` digest `sha256:df26ab5f13b92ab683626ac18ef30ad489d2514e3ee29233b2dcdddac49c90fb` |
| Alembic head | `f058_dashboard_dataset_flags`（与 A 现网 `alembic current` 同名；A 镜像 digest 不同，不能说是同一构建） |
| OpenFGA 镜像 | 待 2.5 hop 写进 compose 后回填 |

A 现网（对照，**不是**本次演练包）：backend `bisheng-backend-shougang:v2.5.0-amd64-test` digest `sha256:205a3c29…`（2026-09-06）；frontend digest `sha256:f95cebbb…`（2026-09-03）。

---

| 角色 | 姓名 | 日期 | 签字 |
|---|---|---|---|
| 客户业务负责人 | （默认同意，未手签） | 2026-09-10 | 交付代记「同意」 |
| 客户身份/组织负责人 | （默认同意，未手签） | 2026-09-10 | 交付代记「同意」 |
| 客户运维负责人 | （默认同意，未手签） | 2026-09-10 | 交付代记「同意」 |
| 毕昇交付负责人 | | 2026-09-10 | 按我方建议关闭 |
