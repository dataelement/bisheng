# F062 Tasks 评审报告

> 历史记录：2026-09-09 用户后续取消 UNKNOWN 冻结及部门同步/筛选；当前语义以 [0.3.0 修订](./usage-and-search-revision.md) 和 design.md 为准。本页旧测试结果不代表修订后的验证结果。

> 当前有效修订：用户确认每用户可配多个模型，各模型独立月额度；一期不做限流，配置使用有字段说明的类型对象。DM8 实机本轮暂缓。最新代码、契约变更及实际验证结果见 [逐模型修订验收](./model-quota-revision.md)。下文保留此前检查时点的证据，测试数量不与新版相加。

**日期**：2026-09-09<br>
**依据**：sdd-review tasks 21 项检查；当前版本为 v3.0.0-beta2，采用当前 release-contract，不沿用技能模板中的旧版本路径。<br>
**对象**：[tasks.md](./tasks.md)、[spec.md](./spec.md)、[design.md](./design.md)、[版本契约](../release-contract.md)。

LGTM

## 结论

114 项任务、8 个 Wave、46 个纯测试任务；覆盖 34 条 AC、26 个 HTTP 端点、8 张新增表及用户资料版本字段。已核对任务依赖拓扑、测试先行、文件范围、六类分区与完成条件。任务计划通过，不代表业务已实现或测试已运行。

## 本轮自查修正

- 将错误文案源改为现场核对的 packages/locales/src/api_errors/{zh-Hans,en,ja}.json；不是猜测的 locales 目录，也不编辑生成产物。
- 根据现有 user/api/user.py 与 UserDepartmentService.change_primary_department，拆出用户资料版本DDL、改名/删除业务接线、主部门事务钩子；不只在新增DSH服务中假设所有资料变更已经通知。
- Gateway 沿用现有 Java17/Spring/MyBatis；DSH mapper须显式注册。Python新独立表遵循现有模型发现/create_all，已有用户表新增列才做revision。
- Platform按现行规则不新增被冻结的react-query v3 import；DSH桌面不是本仓Recoil Client，实际桌面源码/版本留在联调依赖中。

## 21 项检查

| # | 检查 | 结果与证据 |
|---|---|---|
| 1 | AC追溯完整 | 通过：34条AC均有测试任务；末尾逐条索引 |
| 2 | 测试AC标注 | 通过：46个测试任务均显式逐条列举AC |
| 3 | Test-First | 通过：所有后端实现直接依赖前置配对测试 |
| 4 | 依赖正确 | 通过：ID均存在、顺序及Wave无逆向/环 |
| 5 | 原子化 | 通过：每项1–2个直接文件；发现更大范围须继续拆解 |
| 6 | 自包含 | 通过：每项含绝对路径、逻辑、测试上下文、依赖及完成条件 |
| 7 | 粒度/分区 | 通过：业务按单一责任切片，前后端不合并实现任务 |
| 8 | 执行顺序 | 通过：基础设施优先→授权/身份→模型账本→Worker→API→UI→验收 |
| 9 | 无重复实现 | 通过：相同文件仅明确增量：模型执行后补遥测，用户入口接入资料钩子 |
| 10 | API/模型/Service覆盖 | 通过：26端点、8新增表、用户版本字段、领域方法及页面有责任索引 |
| 11 | 前驱接口清晰 | 通过：身份Snapshot、席位/会话、quota/operation方法与冻结契约明确 |
| 12 | 无过度工程 | 通过：不增加第九张表、额外HTTP、桌面本体源码或二期额度特性 |
| 13 | AC格式 | 通过：逐条AC-NN，无范围缩写 |
| 14 | 测试纯净 | 通过：测试项仅断言/fixture或验收报告，不混业务实现 |
| 15 | 无延期债务 | 通过：无TODO/FIXME/HACK；缺真实环境保留验收门禁而非伪通过 |
| 16 | 数据库回退 | 通过：新表保留授权/账本，用户字段单独DDL，不混数据回填 |
| 17 | 跨Feature副作用 | 通过：旧License/过滤器、模型缓存、用户/部门事务、Worker配置和平台路由均指定回归范围 |
| 18 | 前端分区 | 通过：Platform实际任务；普通Client不改，DSH外部交付单列 |
| 19 | Worker租户 | 通过：headers→ContextVar，payload归属核对，finally reset，跨租户dispatcher逐租户派发 |
| 20 | 基础设施优先 | 通过：配置/DTO/ORM/DDL/fixture/错误码及三语言均在首个业务测试前 |
| 21 | 测试降级 | 通过：无测试降级；真实双库/旧二进制/恢复测试有独立验收任务 |

## 实际验证与外部依赖

静态脚本验证全部任务ID/依赖顺序、Wave顺序、后端配对测试、每项≤2直接文件及34条AC覆盖；人工核对端点/数据/方法责任索引、回滚、权限与跨仓边界。未运行114项任务中的业务测试，任务勾选全部为空。

目标旧Gateway二进制及真实授权样本、可计量供应商的逐请求证据、Linux DM8环境和DSH客户端版本是最终验收依赖。可以先完成独立实现与测试，但这些证据不足时不得标记兼容、e2e或发布门禁通过。用户本轮“继续”承接了修复后的设计，Tasks阶段无新增强制确认点。

## Wave 1 执行期增补（2026-09-09）

新增 T115 基础契约/持久化测试与 T116 C5注册同步，原任务ID不变，总计116项；每项仍≤2直接文件。T115先记录2通过/4缺实现失败，基础设施完成后10通过；测试不包含服务端业务实现。依赖位置与实际范围已核对。T004/T007保留未勾选，分别等待Maven/真实双库验证，不以独立编译或SQLite代替最终条件。
