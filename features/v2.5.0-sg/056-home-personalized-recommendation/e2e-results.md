# F056 E2E 验证记录

> 日期：2026-07-15
> 状态：BLOCKED（缺少可连通的完整集成环境和当前用户令牌）

## 已完成的可自动验证

- BiSheng 定向单元/集成测试覆盖主部门精确绑定、算法、权限失败关闭、候选池、投影、任务和租户隔离。
- Portal 定向测试覆盖登录/匿名 SSE 分流、影子模式、当前用户 token 降级、更多页一次加载、显式搜索上报和预览入口。

## 未执行的强制场景

- 真实匿名首页与公共缓存。
- 真实登录账号的影子、0%/10%/50%/100% 稳定灰度。
- 个性化上游故障后使用同一用户 token 降级到 `latest_selected`。
- custom ACL、空间级 ACL、即时撤权、投影滞后与无权信息不泄漏。
- 管理端部门绑定/推荐配置真实保存、更多页、搜索与预览入口的端到端遥测。

## 解除阻塞条件

1. 启动已执行 F056 迁移且完成池预热的 BiSheng，并连通 Redis、ES、OpenFGA 与 Celery worker/beat。
2. 启动 Portal，准备匿名、公共空间用户、custom ACL 用户和被撤权用户。
3. 按上述场景留存请求、SSE 事件、权限审计与无敏感信息日志证据，全部通过后才能将本记录改为 PASS。
