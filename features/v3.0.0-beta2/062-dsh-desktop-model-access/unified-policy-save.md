# 模型权限统一保存

日期：2026-09-15。按用户最新确认的小功能流程实施，管理 API、权限合并及席位合同保持原样。

## 验收规则

- 用户、部门、角色均通过搜索框右侧唯一的「保存」提交；输入、失焦、回车与开关操作只修改草稿。
- 三个标签共享提交入口，搜索、分页和切换标签保留编辑，保存覆盖当前弹窗全部修改。
- 用户维度使用额度设置直接授权：额度大于 0 时启用，额度为 0 时关闭；部门、角色继续作为独立授权来源，各来源取最高额度。
- 用户授权状态展示已保存的最终合并结果。个人额度为 0、部门或角色有正额度时，最终状态仍为「已授权」。
- 保存中按钮反馈并防重复提交；已确认的保存刷新对应版本，失败项保留草稿。
- 未决操作保留原 ID，后续保存先查询原操作结果；前台等待有上限。
- 关闭有草稿的弹窗需确认放弃。提交中的操作完成后再解除关闭保护。

## 交互复核

2026-09-16：移除个人授权开关，用户列表保留用户与 ID、最终授权状态、个人月度额度。旧数据中已关闭的个人授权统一显示额度 0；页面读取保持原授权，编辑后点击右上角保存生效。相关组件与跨标签保存测试 40 项通过。以下记录为此前统一保存版本的验证。

| Before | After | Why |
| --- | --- | --- |
| 用户输入和开关自动提交 | 三个维度统一右上角保存 | 提交时机统一 |
| 用户行自身维护草稿 | 弹窗按用户 ID 保存草稿 | 搜索和标签切换保留编辑 |
| 行内保存状态 | 主按钮使用原有 LoadButton 状态反馈 | 沿用 emil-design-eng 的一致反馈原则，保持原布局 |

## 实现边界

用户操作沿用既有幂等 ID 与版本校验。批量提交沿用逐项 API，已完成项保留生效状态；其余项保留草稿。服务端直接版本与有效权限同步根因修复继续保留。

## 验证

- 前端授权、用量和 API 回归 112 项通过；源码及测试 ESLint、i18n、架构守卫、生产构建通过。
- Chrome 隔离模拟接口验收通过：编辑、失焦、回车、切换开关均保持零写入；点击保存跨标签提交，开关关闭时保存额度，关闭重开读回额度及关闭状态。浏览器运行异常为 0，保留原输入组件开发警告。
- 严格类型检查保持三项既有非 DSH 错误。本次沿用后端，线上授权写入及 Desktop 真实调用独立验收。
- 证据：工作树 `outputs/dsh-unified-save-3006-20260915/`。
- 已部署 `9cfe13d2214bab3f279bf8eac31842ea8cd4d8f1` 至 3006，current 为 `releases/9cfe13d22`，回滚点 `615036e0b`。仅重建 frontend；backend healthy，Gateway/worker 继续原进程，重启数均为 0。管理页/原前台/DSH 配置 200，匿名管理查询 401，前端 index 校验一致。
- Chrome 登录态确认用户和部门共用搜索旁的唯一「保存」，标题仅模型名称，用户四列表格正常。线上授权写入为 NOT_RUN。
- 初次激活因服务器 curl 7.29 参数兼容性触发自动回滚；兼容检查命令重新激活同一校验版本后通过。

## 线上遗留操作处理

2026-09-16 只读确认 admin 原操作 `67d1aac0-9665-4725-beca-048f177375cd` 始终未提交，策略仍为关闭、额度 0。维护事务将该操作终止为 `FAILED / legacy_operation_cancelled` 并清空策略的 `pending_operation_id`；授权、额度及用量保持原值，后台工作进程恢复后未再调度该操作。页面同步移除「待保存」行内状态，统一保存按钮继续承载草稿提交。
