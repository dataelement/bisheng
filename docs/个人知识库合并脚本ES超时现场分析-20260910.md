# 个人知识库合并脚本 ES 超时现场分析

核查时间：2026-09-10 10:50—10:56（容器时间）。按用户说明，核查其提供的测试环境 Pod `bisheng-backend-shougang-75bb4c55b7-9k6bz`；平台路径中的项目标识为 `stpaas-tenant-skm-prod`。本轮仅登录、读取报告、查询数据库、查询 ES 和检查 MinIO 对象存在性，没有部署、刷新索引、执行迁移或清理数据。

## 结论

最新一次失败发生在删除旧目标后的 ES 刷新确认阶段：客户端等待约 10 秒未收到响应，脚本按保护逻辑中止。现场的旧目标 ES 切片已消失，来源和新副本的数据库记录、原文件、ES 切片仍在。不能将这次异常解释为删除完全没有执行，也不能将迁移认定为已经完成。

容器中的 `_refresh_es_after_delete()` 仍直接调用 `store.client.indices.refresh(index=index_name)`，没有本地后来增加的 60 秒请求选项。因此，60 秒版本尚未部署至该容器。

当前查询未发现 ES 持续过载、磁盘写满、分片不可用或刷新队列积压。事发时服务端为何没有在 10 秒内响应，尚缺 ES 节点历史日志和当时的网络/线程指标，不能确定归因为磁盘、网络或线程阻塞。

## 两次失败的区别

| 时间 | 旧目标 | 失败位置 | 报告中的后续行为 |
|---|---|---|---|
| 09-09 21:04 | 文档 35695 / 文件 126739 | 删除后立即计数仍为 ES 367、Milvus 0 | 旧版继续清理对象、权限、版本图和数据库记录，均记录成功；保留新副本 126741 |
| 09-10 10:27 | 文档 35697 / 文件 126741 | ES refresh 请求超时 | 新版立即停止，只记录 indexes 失败；来源未删除，保留新副本 126806 / 文档 35750 |

09-09 的立即计数异常存在刷新可见性延迟的可能；当前旧切片已不存在，不能单凭此结果还原当时每一步的索引可见性。

现场报告：

- `/app/migration_reports/merge_personal_spaces/merge-436227b1aa4a4bef9c89cd4379012469.jsonl`
- `/app/migration_reports/merge_personal_spaces/merge-d54869d6e9f44c9190f2593edf78ddca.jsonl`

第二份报告的 `overwrite_finished`（10:27:38.392573）仅包含 indexes 失败；`unit_failed`（10:27:38.507962）明确记录 `source_deleted=false` 及保留目标文件 126806。

## 实际数据状态

以下由按 ID 查询数据库、目标/来源 ES 索引聚合计数以及 MinIO 原对象存在性检查获得：

| 文件 ID | 所属库 | 数据库记录 | MinIO 原文件 | ES 切片数 |
|---|---:|---|---|---:|
| 108654（来源） | 5301 | 存在，status=2 | 存在 | 367 |
| 126739（第一次旧目标） | 5300 | 不存在 | 不存在 | 0 |
| 126741（第二次旧目标） | 5300 | 存在，status=2 | 存在 | 0 |
| 126806（第二次新副本） | 5300 | 存在，status=2 | 存在 | 367 |

文档 18214、35697、35750 存在，35695 不存在。第二次旧目标处于“数据库记录/原文件存在，但 ES 切片为空”的中间状态。上述检查未重新验证 MinIO 文件字节内容、全部权限元组或 Milvus 当前切片，不等同于完整迁移验收。

## 存储路由与 ES 当前状态

数据库及迁移快照一致：

- 来源库 5301：ES/Milvus 名称 `col_1787534670_fb3811a6`。
- 目标库 5300：ES/Milvus 名称 `col_1787534670_8b6ef0a7`。
- 容器不存在 `bisheng/knowledge/rag/shared_space_storage.py`。本次按库独立存储操作，不能归因为新共享存储迁移流程。
- 两个 ES 索引均为 1 个主分片、1 个副本，默认 `refresh_interval=1s`。
- 目标索引约 2.7 MB、457 条文档；主分片在 es3（10.171.0.61），副本在 es2（10.171.0.60）。请求入口 es1 为 10.171.0.59。
- ES 8.17.3，集群 `green`，3 个节点，0 未分配分片，0 pending tasks，未发现执行中的 refresh 任务。
- 查询时 refresh/search/write 线程池 active、queue、rejected 均为 0；节点 CPU 约 3%—7%，磁盘有余量。集群约 4454 个活动分片，属于后续容量评估线索，不是本次超时的已证实根因。
- 当前目标主分片累计 refresh 时间 37 ms，副本约 342 ms；这些累计指标不能覆盖请求协调等待、网络等待或完整还原历史故障。

## 后续处理建议

1. 先将本地经过测试的独立脚本更新到该容器，核对 60 秒刷新请求选项实际存在。当前尚未部署。
2. 保留本次两份报告及数据状态记录；不要把“超时”当作没有发生删除，也不要手工清空仍完整的来源。
3. 继续执行前按单用户核对迁移计划，明确处理中间状态旧目标 126741 与已保留副本 126806 的顺序。重新整批执行会再产生新副本，不能代替状态核验。
4. 若进一步优化脚本，优先考虑删除后短暂只读轮询计数，仍有残留才刷新；刷新超时后再以只读查询确认状态。不可无条件忽略超时或把未知结果当成功。此方案本轮未实现。
5. 要确定事发时延迟的底层原因，需要查看 es1/es2/es3 在 10:27:27—10:27:38 附近的服务端日志与监控。本轮没有这些历史数据。

## 登录排查更正

通过 DevOps 主平台登录和正常单点登录后，WebSocket 终端连接成功并可执行命令。普通 HTTP 请求终端 `/exec` 返回 400，不能单独证明 WebSocket 终端服务故障；上一轮据此判断平台终端故障不够准确。本次未保存密码或会话令牌到报告中。

## 后续部署记录：2026-09-10 11:02

用户明确授权上传新版后，已更新同一 Pod 的 `/app/scripts/merge_personal_knowledge_spaces.py`。传输改用分块方式；完整内容校验及语法编译通过后，备份原文件并原子替换，保留原有 0644 权限和 root 所有者。最初未完成的传输被停止，确认旧脚本哈希未变后才重新传输。

- 旧脚本备份：`/app/scripts/merge_personal_knowledge_spaces.py.bak-20260910-110248`。
- 旧 SHA-256：`99c60aeeb30074f00504d830e73a3f67db1fdec596d2ae33d89215cfa9427250`。
- 新 SHA-256：`0c53ef2233408974460c4e69b199b23a765a42251040d5c0982f230203e3afcd`，与本地文件一致，158264 字节。
- 容器内运行 `python /app/scripts/merge_personal_knowledge_spaces.py --help` 退出码为 0。
- 读回确认包含 `request_timeout=60, max_retries=0, retry_on_timeout=False`。

本次只替换独立脚本，没有运行预览或 `--apply`，没有重启服务，也没有清理迁移现场数据。

## 后续故障：2026-09-10 11:06 复制写入超时

用户随后执行的 `merge-3fdd432744014ad880798ea1ce9ee1d2.jsonl` 在 `document:18217` 停止。源文件 `108657` 位于空间 5301，新副本 `126812` 位于 5300。

已从容器 `/app/data/bisheng.log` 核实：11:06:17.980 的 `copy_done pk_size=419` 表示 Milvus 写入结束；随后 `copy_normal → copy_vector → insert_es → bulk` 请求 ES `10.171.0.59:9200/_bulk`，约 10 秒后发生 `ConnectionTimeout`（`read timeout=9.9931830316782`）。11:06:28.164 的 `source=108657 new=126812 e=Connection timed out` 是底层原因。底层将副本状态改为 3，脚本检测到源状态 2 与副本状态不兼容后停止。

本次并非旧目标删除刷新失败：之前的 60 秒只作用于覆盖清理刷新，业务 `insert_es` 的写入与刷新仍使用默认客户端。`delete_es ... deleted:0` 是随后清理失败副本的输出，不是超时起点。JSONL 记录 `source_deleted=false`、`target_cleanup_succeeded=true`、`irreversible_overwrite_started=false`。这些是执行报告的记录，不等同于所有存储的事后全面审计。

本地修复在独立脚本内为同步复制调用临时适配 `insert_es` 客户端，写入和刷新均使用 `request_timeout=60, max_retries=0, retry_on_timeout=False`；调用结束恢复函数和客户端，其他线程走原函数。来源索引恢复复用此适配。复制失败报告同时带上目标 ID 和 `remark`，保留底层异常；仍禁止把失败状态当作成功。没有修改容器业务模块。

11:14 事后只读查询发现：数据库中源文件 `108657` 仍在、状态 2，副本 `126812` 已不存在；源 ES 419 条，副本 ES 也有 419 条。说明先前 `deleted:0` 和 `target_cleanup_succeeded=true` 不能证明真正清理完成，存在超时写入随后完成或刷新可见性延迟的可能，具体时序尚未证实。现有副本 419 条为待核查清理项，本轮没有执行删除。

因此同时修复失败补偿：底层返回不兼容状态时，将副本视为存储结果不确定，停止自动清理其索引、对象和数据库记录，报告明确标记保留及需要核查。该保守处理避免再产生“数据库记录已删除，但超时 ES 写入仍完成”的悬空索引；来源保持保留。相应测试覆盖失败副本不继续删除。

相关四个测试文件共 65 项通过（18 条已有依赖/序列化警告），包括真实 `insert_es` 函数的写入、刷新客户端及失败原因传递测试。延长超时仅缓解默认等待过短，ES 请求为何偶发超过 10 秒仍需结合 ES 服务端同期日志分析；尚未用新版本执行真实合并。

11:16 已沿用用户上传授权更新同一容器的独立脚本。备份为 `/app/scripts/merge_personal_knowledge_spaces.py.bak-20260910-111606`，新 SHA-256 为 `8c03fe88eda7920d07c72aec30a900501b08bb4cedc72dd3c3912e6d91294450`（160177 字节）。容器文件读回哈希一致、语法检查通过、`--help` 退出码 0。本轮没有执行合并或残留索引删除。

## 后续故障：2026-09-10 11:24 来源清理校验

报告 `merge-4dfebda610024522bdf92e29c169c1cd.jsonl` 的 `document:18271` 对应源文件 `108716`（5301）和副本 `126835`（5300），源索引快照为 ES 27、Milvus 27。

容器日志显示：11:24:16.950 源 ES 删除返回 `total=27, deleted=27, failures=[]`；11:24:17.522 来源计数查询后立即进入补偿，期间没有来源 ES 刷新。原校验以索引非零作为短路条件，未再读取来源权限，即已进入失败流程。因此本次失败条件来自索引，但旧日志没有 ES/Milvus 的实际校验计数，无法唯一确认哪个引擎短暂可见。删除后约 0.57 秒校验、未刷新 ES 的时序与删除可见性延迟吻合，此为高可能解释，不应当作已唯一证实的 ES 根因。

补偿期间重新复制源索引 27 条并刷新 ES，随后删除目标。11:26 只读查询确认：源数据库记录状态 2、来源版本关联 `108716 → 18271` 存在，源 ES 27 条；副本数据库及版本关系已不存在，副本 ES 0 条。本轮未重新核查其 MinIO、Milvus 和权限全量状态。报告中的 `rollback_record_write_failed` 是通用补偿路径写入的固定原因码，实际异常为来源清理校验失败，不代表这次 JSONL 写入出错。

修复限定在独立脚本的来源清理校验：首次零残留立即通过；检测到残留时分别等待 0.25、0.5、1 秒复查全部计数，ES 持续可见则用现有 60 秒刷新后再核验。真实残留或刷新异常仍停止，明确记录 `milvus_count`、`es_count`、`permission_count` 和源 ID。回归覆盖即时完成、可见性延迟、刷新后归零、三类真实残留、刷新失败。

本轮相关五个测试文件 72 项通过，Ruff、格式和语法检查通过。11:29 已备份并更新容器同一路径：备份 `merge_personal_knowledge_spaces.py.bak-20260910-112901`；新版 SHA-256 `a73371ab218104640c5547e0ff525b5fa7dd19f68537a0f0932fa95ae8506da7`，161398 字节。容器读回哈希一致，`--help` 退出码 0。未运行新版本合并，也未执行历史残留清理。

## 11:33—11:36 容器只读复核

用户要求登录检查后，再次连接原 Pod。容器脚本哈希仍为 `a73371ab...8506da7`，无合并进程，最新报告仍为 11:24 的 `4dfebda610024522bdf92e29c169c1cd`；尚无 11:29 新版执行记录。

报告记录 18 个单元成功、1 个失败、6 个尚未处理。针对成功的全部 18 个文件，现场查询确认：来源数据库记录均已不存在；目标数据库状态均为 2、版本关系和原文件对象均存在；来源 ES/Milvus 计数全部归零，目标两种索引计数均与迁移前快照一致。本次没有逐项复核这 18 个目标的标签及权限。

| 对象 | 数据库/版本关系 | 原文件对象 | ES | Milvus | OpenFGA 文件关系 |
|---|---|---|---:|---:|---|
| 已恢复源文件 108716 | 存在，状态 2，document 18271 | 存在 | 27 | 27 | parent 与 owner 共 2 条 |
| 本次失败副本 126835 | 不存在 | 不存在 | 0 | 0 | 0 |
| 先前失败副本 126812 | 不存在 | 不存在 | 419 | 0 | 0 |

当前明确待处理事项为源文件 108716 加 6 个未执行单元，以及 126812 的 419 条孤立 ES 记录。此次仅做查询并更新本地分析文档，未执行合并、索引删除或容器文件修改。

## 11:46 源索引缺失与单元失败继续处理

最新报告 `merge-d93a42f41f904a55b6da87c0337822b9.jsonl` 的 `document:35151` 来源为文件 `126186`、来源库 5301；执行前快照明确记录 ES 58 条、Milvus 0 条。容器 `copy_vector` 从来源 Milvus 读取内容并据此写入目标 Milvus/ES，因此这份文件复制得到目标 ES 0 条。11:46:30 日志 `copy_done pk_size=0` 与此吻合。这次是来源索引不一致，不能当作 ES 复制超时或删除刷新延迟。失败副本为 `126841`，报告记录来源未删除、补偿完成；本轮未重新进行它的跨存储全量核验。

按用户要求调整默认错误处理：复制前发现来源 ES/Milvus 数量不同，先短暂复查；持续不同则跳过整个单元，不创建文件副本，保留来源并记录 `source_index_count_mismatch`。来源缺失向量须另行修复或重新解析，脚本不会自动生成缺失数据。目标复制后数量不匹配也先短暂复查，必要时刷新 ES，仍不匹配则保持失败。

普通单元失败仅在来源已保全、补偿完整、审计正常且未开始不可逆覆盖时继续后续候选，含失败项的旧库保留。继续处理不等于忽略失败：汇总为 `completed_with_errors`，退出码 3，并输出失败数量；源数据异常跳过为 `completed_with_skips`、退出码 0。日志/报告无法落盘、来源恢复不完整、覆盖中断或删库失败仍停止。可使用 `--stop-on-error` 恢复遇错即停。原批次失败测试改用此显式参数，保留旧模式验证。

本地相关七个测试文件共 82 项通过（18 条已有警告），覆盖复制/权限/校验/来源删除失败后处理后续文件，源索引异常复制前跳过，审计及补偿失败阻断继续，目标可见性复查与真实缺失。Ruff 检查通过。本轮未执行真实合并、源索引修复或历史残留删除。

11:58 已更新原容器独立脚本，旧文件备份为 `/app/scripts/merge_personal_knowledge_spaces.py.bak-20260910-115844`。新版 SHA-256 为 `66f11288252d17432604dba87c9dffe453331f0c7f89851cdd499ca897a3b50b`（165517 字节）；容器读回哈希一致，语法检查通过，`--help` 退出码 0 且列出 `--stop-on-error`。未执行新版本 apply。

## 12:22 原记录迁移与解析失败降级

用户进一步明确：目标是合并重复个人库，不再因索引问题跳过文件。此前 11:58 的“来源索引不一致则跳过”策略由本节替代。

默认执行路径改为 `Backend.execute_records`：保留原文件 ID、文档/版本 ID、主版本及 MinIO 对象地址，事务内更新所属知识库和目录；同名旧文件与完整旧版本链在同一事务删除。Milvus 无可读数据时尝试 ES；索引读写、权限及附属资源异常收集到报告，迁入记录设为 status=3，后续可按需重新解析。ES 无向量时只保留全文，不调用嵌入模型补算。被覆盖文件对象也暂留并列出回收清单，防止复用对象误删。

空旧库业务清理异常时，重新确认没有文件/文档后事务移除库记录与作用域；外部残留写入 `source_space_cleanup_pending`。解析不会自动修复权限、旧索引或对象残留，这些仍按清单处理。数据库提交、版本图损坏、审计失败、活动解析/审批/引用等保护仍保留，不把真正未迁入的内容标为成功。

本地原有及本轮相关回归合计 103 项通过（102 项联合回归通过后新增 1 项整批用例单独通过），包括真实 SQLite 事务中的原 ID/对象/版本链保留、同名覆盖、事务回滚、异常索引/权限降级、空库删除及最终报告。ES/Milvus/OpenFGA 写入使用替身；Ruff 与编译检查通过。真实 MySQL/DM8 事务及实际合并写入仍未执行。

12:22 已更新原容器 `/app/scripts/merge_personal_knowledge_spaces.py`，191017 字节；SHA-256 `54f9260249fd8e2a5f03e89d701d5a1f7c627e05f2ed999ed08be616eaeb5748`，容器读回一致。旧版备份 `/app/scripts/merge_personal_knowledge_spaces.py.bak-20260910-122255`。语法检查通过，容器 `--help` 退出码 0。

12:24 使用新版读取函数只读核验文件 126186（来源库 5301，原状态 2）：Milvus 0 条；ES 成功读出 58 条，正文存在、向量不存在。已证实此次失败可以使用 ES 读取回退，实际迁入会标记解析失败。此轮未执行 `--apply`、原文件删除或索引数据写入。
