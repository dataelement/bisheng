bisheng {{ .Chart.Version }}

Frontend Service: {{ include "bisheng.frontendService" . }}:{{ .Values.frontend.service.port }}
Backend Service:  {{ include "bisheng.backendService" . }}:{{ .Values.backend.service.port }}

{{- if .Values.sandbox.enabled }}

Sandbox discovery (worker):
  pattern={{ .Values.sandbox.discoverHostPattern }}
  index_start={{ .Values.sandbox.discoverIndexStart }}

Install the sibling chart first, for example:

  helm upgrade --install sandbox docker/helm/bisheng-sandbox \
    --namespace bisheng-sandbox --create-namespace

Keep SANDBOX_TOKEN / BS_SANDBOX_CONF__TOKEN identical on both releases.
{{- end }}
