# F062 用量缺失与用户检索修订

2026-09-09，用户明确要求取消未知用量冻结，并取消部门数据同步和筛选。

- 缺少 usage：明细保留 `USAGE_UNKNOWN`、nullable token字段及缺失原因，不累加估算用量，不冻结用户或模型。成功 JSON/SSE 正常返回；上游实际错误维持原错误语义。
- 逐用户、逐模型独立额度继续按已记录用量判断。未知用量诊断集合与准入保护集合分离。策略同步、账本持久化不确定、主切换等独立保护继续生效。
- Gateway 不接收、不保存、不筛选 DSH 部门；删去席位表字段及索引、内部 DTO 参数、BiSheng 部门读取及变更同步钩子、管理页输入框/列。用户名检索及资料版本同步保留。
- 客户端契约升级 0.3.0，旧 JSON 样例保留为历史；客户端需要支持成功回答 usage 字段为 null。尚无客户端团队确认或真实联调结果。
- 可选可靠补记工具保留，继续调用不依赖补记。

## 本轮验证

- 后端 DSH 分组回归：323 个不同用例通过，17 项跳过，未计入通过数。分组使用真实隔离 MySQL、Redis 和本地 HTTP/ASGI 链路；SQL 夹具生命周期不同，分别运行普通组和 external 参数组。两个旧测试仍按共享额度预期阻断另一模型，已改成同模型达限拒绝、其他模型独立可用并复验。
- 最后针对结算、准入、恢复、模型服务及 HTTP/SSE 的 47 项回归全部通过，覆盖恢复完成后 UNKNOWN 不阻断、补记的确定性版本冲突不误触发存储隔离。
- Gateway：58 项全部通过，包含五万席位分页、资料版本、路由、现有 License 行为及 SQL 可移植性检查；无 DM 实机测试。
- Platform：7 个测试文件、32 项通过；覆盖无部门输入/请求字段、分页和用户名防抖。修复初始化空关键词防抖重复重置分页的问题。
- 前端 workspace `pnpm lint`、`pnpm typecheck`、`pnpm check-i18n` 均通过。DSH 后端模块、测试和资料 Repository 的 Ruff 检查通过；架构守卫 151 个改动源文件无 VIOLATION。整个既有改动范围的 Ruff 仍有历史问题，用户服务本轮未新增 lint 项；不声称全仓 Ruff 通过。
- 两仓 `git diff --check` 通过；HTML 20 张表列数一致，客户端保留 7 张时序图，模型调用图补充 nullable usage 的正常结束语义。

## 交付边界

代码和文档保留在各自未提交工作区，未提交、推送或部署。客户端 0.3.0 的 nullable usage 行为、接口说明和样例已在本地更新，尚未向客户端团队发送消息或取得确认。真实 Desktop/Nginx/供应商联调、客户旧 License 制品与发行工具验收仍未完成；DM 实机按用户要求暂缓。
