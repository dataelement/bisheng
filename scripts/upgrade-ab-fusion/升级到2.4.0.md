# 升级到 2.4.0（从当前测试环境 B 的 2.2）

适用：测试机 `10.168.24.121`，现网镜像 `v2.2sgv260402-JiTuan`，compose：`/data/bisheng-main/docker/docker-compose.yml`。  
**做到 2.4.0 为止，不要跑 2.5.0-sg，也不要做 A→B 融合。**

官方依据：《版本升级注意事项》里「升级到 2.3.0-beta1」「升级到 2.3.0」「升级到 2.4.0-beta1」「升级到 2.4.0」。  
可跳过（只有换镜像、无 SQL）：2.3-beta2/3/4、2.4-beta1-fix。

注意：B 不是官方 `v2.2.0`。每步 SQL 前若列已存在就跳过。脚本包会做幂等判断：先把 `scripts/upgrade-ab-fusion` 拷到 B。

这台机是 CentOS 7，若 `docker compose` 不存在，把命令改成 `docker-compose`。

---

## 0. 笔记本拷脚本，SSH 进 B

```bash
cd /Users/lkk/workplace/bisheng
scp -P 52012 -r scripts/upgrade-ab-fusion root@10.168.24.121:/data/upgrade-ab-fusion
ssh -p 52012 root@10.168.24.121
```

compose 路径由 `lib/discover.sh` 自动发现，不用改脚本。镜像和容器名有默认值，换值就 `export`。每个 hop：先 SQL，再改 compose，再 `compose up`（按新 tag 自己拉镜像）。

P2 hop 跑起来就升级。演练机跳过 digest 时：

```bash
cd /data/upgrade-ab-fusion
export DRILL=1   # 仅演练机
bash p2/00-precheck.sh
```

看到 `预检通过` 再继续。`auth_type` / `file_level_path` 若已是 PRESENT，说明已经到过 2.4，不要重跑 ALTER。

---

## 1. 备份（必须）

```bash
cd /data/upgrade-ab-fusion
bash p2/01-freeze-and-backup.sh
```

MinIO / Milvus / ES 目录还要按 `BACKUP_DIR` 里 `TODO-storage.txt` 另拷一份，和 MySQL 同一冻结点。

官方 2.3-beta1 要求：`convert_all` 之前应已做过 2.2 的知识库修复。若从没跑过，在**当前仍是 2.2 镜像、未改 compose 之前**：

```bash
docker exec -it bisheng-backend bash
env PYTHONPATH="./" python bisheng/script/knowledge_data_fix.py
env PYTHONPATH="./" python bisheng/script/knowledge_data_fix.py --mode fix_all
```

路径若报没有，试 `scripts/knowledge_data_fix.py`。跑完再升 2.3。

---

## 升级到 2.3.0-beta1

### 数据库操作

由 `p2/10-hop-2.3-beta1.sh` 执行官方列：`knowledge.metadata_fields`，`knowledgefile` 的 `user_name/updater_id/updater_name/user_metadata`，回填后 `DROP extra_meta`。

### 修改 docker-compose.yaml

backend / worker / frontend 改为：

```YAML
image: dataelement/bisheng-backend:v2.3.0-beta1
image: dataelement/bisheng-frontend:v2.3.0-beta1
```

### 重启并转换知识库元数据

`convert_all` **必须用 2.3-beta1 容器**，不要用 2.4/2.5 镜像跑。脚本会自己 pull、改 compose、起容器、跑 convert_all。

```bash
cd /data/upgrade-ab-fusion
bash p2/10-hop-2.3-beta1.sh
```

等价手工：

```Shell
docker-compose pull
docker-compose up -d
docker exec -it bisheng-backend bash
sh bisheng/script/knowledge.sh convert_all
```

---

## 升级到 2.3.0

跳过 beta2/3/4。

### 数据库操作

给历史角色补工作台、管理后台菜单（脚本已做成可重跑）：

```SQL
INSERT INTO roleaccess (role_id, type, third_id)
SELECT id, 99, 'frontend' FROM role
WHERE ... 不存在则插入;

INSERT INTO roleaccess (role_id, type, third_id)
SELECT id, 99, 'backend' FROM role
WHERE ... 不存在则插入;
```

### 修改 docker-compose.yaml

```YAML
image: dataelement/bisheng-backend:v2.3.0
image: dataelement/bisheng-frontend:v2.3.0
```

### 重启并跑遥测脚本

脚本会自己 pull、改 compose、补菜单、起容器、跑遥测脚本。

```bash
cd /data/upgrade-ab-fusion
bash p2/11-hop-2.3-release.sh
```

手工等价：`telemetry_events_reindex.sh`、`mid_table.sh`（在 2.3 容器里）。

---

## 升级到 2.4.0-beta1

### 数据库操作

```SQL
ALTER TABLE message_session ADD COLUMN group_ids json null;
-- 再按用户组回填；脚本只更新 group_ids IS NULL 的行
```

### 修改 docker-compose.yaml

```YAML
image: dataelement/bisheng-backend:v2.4.0-beta1
image: dataelement/bisheng-frontend:v2.4.0-beta1
```

```bash
cd /data/upgrade-ab-fusion
bash p2/20-hop-2.4-beta1.sh
```

---

## 升级到 2.4.0

### 数据库操作

官方 2.4.0 的 ALTER（知识空间列、用户头像、config longtext、tag、roleaccess knowledge_space 等）由 `p2/21-hop-2.4.sh` 按列是否存在添加。

### 修改 docker-compose.yaml

```YAML
backend:
  image: dataelement/bisheng-backend:v2.4.0
backend_worker:
  image: dataelement/bisheng-backend:v2.4.0
frontend:
  image: dataelement/bisheng-frontend:v2.4.0
```

```bash
cd /data/upgrade-ab-fusion
bash p2/21-hop-2.4.sh
```

起 2.4 后应有 `space_channel_member`（2.4 仍靠 create_all 建新表）。

### 数据兼容 SQL（个人知识库 type=2）

测试机已盘点：`knowledge.id=17`，type=2，2 个文件。按官方转成知识空间，但**保留原名称**（官方把名字统一改成「个人知识空间」会撞名，脚本没照抄改名）。

```bash
cd /data/upgrade-ab-fusion
CONFIRM_TYPE2=1 bash p2/22-hop-2.4-type2.sh
```

---

## 怎么算升到 2.4 了

```SQL
SHOW COLUMNS FROM knowledge LIKE 'auth_type';
SHOW COLUMNS FROM knowledgefile LIKE 'file_level_path';
SELECT type, COUNT(*) FROM knowledge GROUP BY type;
-- type=2 应为 0
SELECT COUNT(*) FROM space_channel_member;
```

```bash
docker inspect bisheng-backend --format '{{.Config.Image}}'
# 应为 dataelement/bisheng-backend:v2.4.0
```

登录、打开原来的工作流和传统知识库。  
**到这里停。不要跑 `升级到2.5.0-sg.md`，不要跑 F006。**
