# DSH 管理界面调整（2026-09-10）

参考：[DSH Desktop 接入演示 v0.8](https://claude.ai/code/artifact/8ab14900-e95b-42dd-8a5c-3f2750e046b0)。demo 仅作视觉与交互参考，以当前 spec/design 和用户后续确认的边界为准。

## 已获用户批准

- 模型管理中增加“DSH 开放范围”，先选择模型，再按用户名搜索毕昇用户并配置该模型的月度 Token 额度。用户明确批准模型维度入口及配套查询接口。
- 使用提供方名称与实际 `model_name`，不显示自动生成的 `model 1/2/3` 配置标签。
- 未登录过 DSH、尚未占席的当前租户用户可以提前授权；授权不分配席位。
- 登录会话通过席位列表的弹窗展示，保留原有分页、异常状态与关闭取消请求。
- 原系统页保留席位、用户用量查询和操作审计；模型额度编辑统一移至模型管理。

## 不从 demo 恢复的功能

| demo 行为 | 本期边界 |
|---|---|
| 部门、用户组授权及叠加取最大值 | 仅用户＋模型独立配置，不查询或同步部门 |
| RPM、TPM、租户默认额度、留空继承 | 不限流，无继承；显式非负整数月度额度，0 不可调用 |
| 免费 10 席、过期回落、按有效令牌占席 | 保留已确认的 License 和固定席位规则 |
| 个人令牌台账、批量吊销、技能包令牌 | 保留既有 DSH 会话与单人撤席，不引入 PAT |
| 二期用量图表 | 不新增；保留现有用量及未知用量明细 |
| 新接入开关、下载地址编辑、密钥验签表单 | 不新增部署或管理配置 |
| 新深链/登录协议 | 7 个客户端接口、HMAC 与 PKCE 协议不变 |

## 接口与一致性

新增 `GET /api/v1/dsh/admin/models/{model_id}/users`，普通管理员 JWT；可选 `tenant_id`、`cursor`（上页最后 user_id）、`limit`（1–100，默认20）、`keyword`（用户名，最多128字符）。使用现有管理员作用域及目标模型可见性校验。Root 未选择子租户时使用当前租户，不能返回跨租户用户。

返回 `model: {id,name,is_root_shared}`、`tenant_id`、`items`、`next_cursor`、`has_more`。`items` 每项为 `{user_id,user_name,version,enabled,monthly_token_limit,pending_operation_id}`。列出当前租户的有效用户，包括无策略/无席位/从未登录 DSH 的用户；前端按当前 model_id 展示已授权状态与额度。只读取一页用户及对应策略，不扫描全部用户、不读取部门或 Gateway 席位数据。

保存使用 `PUT /api/v1/dsh/admin/users/{user_id}/models/{model_id}/policy`，读取单行使用同路径 GET。提交 `{operation_id,expected_version,enabled,monthly_token_limit}`，只修改当前模型；不再提交该用户的完整模型列表。操作审计及同意图重试机制保留，但版本与待处理所有者按模型独立。

用户查询由用户模块提供有效用户分页，DSH 模块仅查询当前模型的本页策略；无记录返回 version=0、enabled=false、monthly_token_limit=0。查询参数 `authorized_only=true` 先按额度表的模型索引分页，再通过用户模块校验有效性及用户名，不能以登录或席位筛选。分页游标以授权索引扫描位置为准，用户失效或用户名不匹配时可返回少于 limit 条，按 has_more 继续。

本次后续表结构修订经用户明确批准，详见 [单行授权修订](./model-policy-row-revision.md)。不新增明细表、Alembic 版本或业务手写 SQL。

## 验收

- 两个提供方下的同名模型可区分，LLM 行显示 DSH 入口，其他类型不显示。
- 按用户名查到从未登录 DSH 的用户，保存模型额度成功且不分配席位。
- 修改/取消模型 A 授权保留模型 B；并发版本冲突、超时重试、处理中重开弹窗均不覆盖未知结果。
- 子租户管理员不能读取或修改其他租户的用户；共享模型只授权当前租户用户。
- 会话弹窗可关闭、分页并在关闭后终止旧请求。

## 本地验证及交付边界

- 后端定向回归：50 项通过、10 项跳过、23 项外部数据库变体未运行。覆盖无策略用户、有效成员筛选、分页、用户名转义、租户隔离、管理员校验、模型名称及既有策略一致性。
- 前端组件回归：21 项通过，覆盖当前模型增删不影响其他模型、版本冲突、同操作重试、关闭后重开及拒绝后的刷新、会话弹窗和只读用量。
- 前端全量 lint、typecheck、check-i18n 通过；变更文件 ruff、架构守卫和 diff 空白检查通过。
- 已补充可选真实 HTTP E2E 用例；本次没有配置外部 E2E 目标和测试清单，因此不宣称真实环境端到端通过。未测试真实达梦实例。
- 按用户最新要求，仅本地提交并推送 `feat/3.0.0-beta2-pre`，不部署或更改 109；部署后按上方验收清单联调。
