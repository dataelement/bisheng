# 复制即跑（只看这一份）

不要看 README 里的表去想下一步。  
打开本文件，从上到下，**一整段复制 → 粘贴回车**。某步报 `FAIL` 就停，不要跳。

- 在哪跑：SSH 进 **B 演练机** `10.168.24.121`（端口 `52012`）
- 生产 B 不要用这份（里面 `DRILL=1`）
- 回滚：`p2/90-rollback.md`

---

## 0. 笔记本上：把整个仓库压缩包拷到 B

2.5 hop 会用压缩包里的 `docker/` **整份覆盖**现场 compose / config.yaml / entrypoint / nginx，只拷升级脚本不够。

```bash
cd /Users/lkk/workplace
tar -czf bisheng-2.5.0-sg.tgz \
  --exclude bisheng/.git \
  --exclude bisheng/src/frontend/*/node_modules \
  --exclude bisheng/docker/mysql/data \
  --exclude bisheng/docker/data \
  bisheng
scp -P 52012 bisheng-2.5.0-sg.tgz root@10.168.24.121:/data/
```

然后 SSH：

```bash
ssh -p 52012 root@10.168.24.121
cd /data
tar -xzf bisheng-2.5.0-sg.tgz
# 升级脚本在解压后的仓库里
cd /data/bisheng/scripts/upgrade-ab-fusion
```

找不到 `docker/docker-compose.yml` 时，显式指定：`export PACK_DOCKER_DIR=/data/bisheng/docker`。

---

## 1. B 上

compose 路径、project 名、config.yaml / entrypoint.sh 的宿主机位置**全部自动发现**（从运行中的容器标签和挂载表反查，见 `lib/discover.sh`），不用填 `env.sh`，换机器也不用改脚本。

P2 hop **跑起来就升级**，不用设 `APPLY` / `CONFIRM_LAYOUT`。演练机跳过 digest 检查时才需要：

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
export DRILL=1          # 演练机专用。生产禁止设置
```

镜像和容器名也都可以用环境变量覆盖，例如 `export BACKEND_CONTAINER=xxx`。每个 hop 先 SQL、再改 compose、再 `compose up`（自己拉镜像）。

`p2/30-hop-2.5.sh` 已钉 D02 digest。B 上若没有该镜像，先 `docker load`，不要改成浮动 tag。

---

## 2. 预检（不改数据）

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
bash p2/00-precheck.sh
```

看到 `预检通过` 再往下。若大量列已经是 `PRESENT`，说明这库升级过，不要继续 hop。

---

## 3. 停服务 + 备份 MySQL

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
bash p2/01-freeze-and-backup.sh
```

---

## 4. 升到 2.3-beta1（含 convert_all）

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
bash p2/10-hop-2.3-beta1.sh
```

---

## 5. 升到 2.3

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
bash p2/11-hop-2.3-release.sh
```

---

## 6. 升到 2.4-beta1

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
bash p2/20-hop-2.4-beta1.sh
```

---

## 7. 升到 2.4

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
bash p2/21-hop-2.4.sh
```

---

## 8. 个人知识库 type=2

演练机已盘过：只有 `id=17` 且有文件，按评审默认转换。

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
CONFIRM_TYPE2=1 bash p2/22-hop-2.4-type2.sh
```

---

## 9. 升到 2.5.0-sg

从 **已是 2.4.0** 升上来。`p2/30-hop-2.5.sh` 会用压缩包 `docker/` 覆盖现场配置（含 OpenFGA）、换 D02 镜像、两段 Alembic。

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
bash p2/30-hop-2.5.sh
```

`alembic current` 必须等于 `f058_dashboard_dataset_flags`。失败即停。不要 `alembic stamp head`。

---

## 10. 权限 F006 + 工作台模型

先看预览：

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
CONFIRM_F006=0 bash p2/31-f006-workstation.sh
```

没有报错再落库：

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
CONFIRM_F006=1 bash p2/31-f006-workstation.sh
```

---

## 11. 计数验收

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
bash p2/40-verify.sh
```

然后自己点：登录、开一条工作流、打开一个传统知识库。  
**不要打开 B 的首钢同步.** A 继续作为唯一同步端.

---

## 12. 融合准备 (B 必须已是 2.5.0-sg; 默认不写 A)

在 **B** 上跑. 本地 mysql 是 B, A 走 SSH (`A_SSH_HOST`, 默认 `10.171.0.50`).
会话里 `export SSHPASS='...'` (不要写入仓库).

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
APPLY=0 bash p4/00-install-control.sh
APPLY=0 bash p5/00-protect-a-baseline.sh
# APPLY=1 bash p4/00-install-control.sh
# APPLY=1 bash p5/00-protect-a-baseline.sh
```

---

## 13. 身份映射 (禁止按姓名合并)

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
bash p4/01-export-identity.sh
bash p4/02-propose-maps.sh
# 复核 logs/p4/*.proposed.csv / *.conflicts.csv / *.manual.csv
cp p4/tenant-map.csv.example p4/tenant-map.csv
cp logs/p4/user-map.proposed.csv p4/user-map.csv
cp logs/p4/dept-map.proposed.csv p4/dept-map.csv
cp logs/p4/role-map.proposed.csv p4/role-map.csv
cp logs/p4/model-map.proposed.csv p4/model-map.csv
cp logs/p4/server-map.proposed.csv p4/server-map.csv 2>/dev/null || cp p4/server-map.csv.example p4/server-map.csv
cp logs/p4/tool-map.proposed.csv p4/tool-map.csv 2>/dev/null || cp p4/tool-map.csv.example p4/tool-map.csv
APPLY=0 bash p4/10-apply-identity.sh
# APPLY=1 bash p4/10-apply-identity.sh
```

`create` 用户密码不迁, 需在 A 重置. B 不要开组织同步; A 继续作为唯一同步端.

---

## 14. B 业务迁入 A (默认 APPLY=0)

A 原知识空间不参与. B 传统库/工作流/助手/会话 INSERT 进 A.

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
bash p5/10-export-b-business.sh
APPLY=0 bash p5/30-apply.sh
# 复核 logs/p5/gaps-model-tool.tsv (B 独有模型/工具须在 A 重配后再填 map)
APPLY=0 bash p5/20-copy-minio.sh
APPLY=0 bash p5/22-copy-vectors.sh
APPLY=0 bash p5/35-apply-openfga.sh
bash p5/40-verify.sh
bash p5/50-retrieve-gold.sh
APPLY=0 bash p5/36-publish-flows.sh
# APPLY=1 bash p5/30-apply.sh
# APPLY=1 bash p5/20-copy-minio.sh
# APPLY=1 bash p5/22-copy-vectors.sh
# APPLY=1 bash p5/35-apply-openfga.sh
```

MinIO 禁止覆盖 A 已有键. 现场容器名不同时: `export B_MINIO_CONTAINER=... A_MINIO_CONTAINER=...`.
Milvus/ES 走两侧 `backend` 容器 (默认 `bisheng-backend` / `A_BACKEND_CONTAINER`). 不兼容库进 `logs/p5/vector-exceptions.tsv`, 未经批准不自动重解析.
部门授权还依赖 B OpenFGA 导出 (`p5/12-export-b-openfga.sh`, 已挂在 `10-export` 末尾). 不可达时只迁 roleaccess 角色授权.

```bash
BATCH_NO=fusion-YYYYMMDD APPLY=0 bash p5/90-rollback.sh
```

---

## 15. 一键编排 (仍然默认不写 A)

```bash
cd /data/bisheng/scripts/upgrade-ab-fusion
bash full-migrate.sh
# APPLY=1 CONFIRM_FULL_MIGRATE=1 bash full-migrate.sh
```

默认不迁积分/审计. 默认不迁 B 的知识空间 (`MIGRATE_B_SPACES=0`).

---

## 还没到 (先不要跑)

- P6 切流量: 见 `p6/cutover-runbook.md` (冻结水位 -> 增量 -> 金标 -> 发布 -> 入口切到 A)
- 不兼容知识库须客户批准后才重解析
- 工作流端到端运行仍须人在 A UI 做; 脚本只改 status
