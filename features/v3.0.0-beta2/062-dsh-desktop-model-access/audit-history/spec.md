# DSH 操作审计历史

2026-09-16，用户确认：将现有功能已经保存的操作记录统一展示在操作审计页。
范围为 F062 内部只读查询和页面补全，采用 Small feature 流程。

| AC | 验收要求 |
|---|---|
| 01 | 进入、刷新、重新打开操作审计页，均从后台读取已有历史，默认时间倒序。 |
| 02 | 汇总管理操作与部门/角色授权变更，包含成功、失败及处理中记录，保留系统同步等已有操作类型。 |
| 03 | 展示时间、操作人、操作类型、对象、模型、结果，展开查看已保存的变更前后内容；失败操作的提交内容单独标明。 |
| 04 | 支持按操作类型、结果筛选及分页；同时间记录跨页保持稳定顺序。 |
| 05 | 沿用当前租户管理权限；历史对象删除后保留记录和 ID；仅返回允许公开的业务字段。 |
| 06 | 加载中、查询失败和真实空结果分别展示；刷新重读最新记录。 |
| 07 | 保留原席位操作状态查询和重试能力；现有授权、席位、Token 统计与原始审计数据保持。 |

本轮读取现有两张表，不补造历史、不扩展新埋点、不改变 Desktop/Gateway 协议。
