"""Legacy manual/dev scripts (not pytest unit tests).

These connect to live services or use requests/asyncio against a
running backend. Kept for reference but excluded from collection.
"""

collect_ignore_glob = ["*.py"]
