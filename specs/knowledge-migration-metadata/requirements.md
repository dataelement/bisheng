# 共享存储迁移修复

Status: Locally verified

用户已确认普通搬迁、保留链接、覆盖合并一起修复，并要求分批处理。

- REQ-1 / AC-1：迁移复用已有分块和向量，不调用解析、切分、Embedding；普通搬迁保留文件、文档、版本与对象标识。以数据库集成测试和存储契约测试验证。
- REQ-2 / AC-2：保留链接覆盖合并时允许规范文档、主版本代次变化，但必须复用来源内容；已有分发入口的归属不能丢失。以合并、恢复回归验证。
- REQ-3 / AC-3：每次领取有限数量单元，批量读取控制数据并回写检查点/结果；失败保留单元级恢复，不能用并发代替批量。以仓储调用计数和中断恢复测试验证。
- REQ-4 / AC-4：源内容缺失、外部写入部分失败时不报成功、不回退到重解析；防止后台投影抢先重建。以失败注入验证。

不执行真实数据迁移，不更改审批规则、覆盖策略或增加数据库表。
