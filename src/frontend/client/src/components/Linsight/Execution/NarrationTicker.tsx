// Moved to neutral ground so the daily-chat thinking group can share it —
// task mode and Chat/Messages must not import from each other.
export { NarrationTicker } from '~/components/ui/NarrationTicker';
