a_dept_pk,b_dept_pk,external_id,action,a_name,a_dept_id,a_source,a_parent_pk,a_short_name,a_sort_order,a_status,a_tenant_id,note
# 按 external_id bind 或 create。禁止按部门名合并。
# bind 必须填 B 的 b_dept_pk。create 的 b_dept_pk 留空, SQL 按 external_id 新建。
# 1001,20,D001,bind,财务,BS@old,sg,,财务部,0,active,1,B 已有相同编码
# 1002,,D002,create,科室A,BS@a2,sg,1001,,0,active,1,B 无此编码
